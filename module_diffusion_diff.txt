diff --git a/dyn_em/module_diffusion_em.F b/dyn_em/module_diffusion_em.F
index baa5c168c..deaf810d8 100644
--- a/dyn_em/module_diffusion_em.F
+++ b/dyn_em/module_diffusion_em.F
@@ -1,58 +1,30 @@
-! WRF:MODEL_LAYER:PHYSICS
 
-!=============================================================================
-!
-! Modification History:
-!
-! Implemented 08/2008 by Jeff Mirocha, jmirocha@llnl.gov
-!
-! 01/22: modified by Xiaoming Shi (https://github.com/shixm-cloud/WRF-RNA)
-!        - ASW summarizing XS modifications
-!        - zeroed and smoothed SFS component in diffusion subroutines for
-!          numerical stability (look for "YW and XS")
-!           - they needed this for a real typhoon case
-!           - ASW found this to be unnecessary for regular PBL cases
-!           - ASW: this weirdly does not affect the results a lot
-!
-! 03/24: modified by Adam Wise (adamwise@berkeley.edu)
-!        - added logic for SFS smoothing by YW and XS
-!        - added heat flux outputs from Jeff
-!        - added MOST surface forcing option from Jeff
-!           - modified his implementation to take surface cooling as a namelist input
-!           - to do this, tke_heat_flux should be set as a negative*1000 of the 
-!             cooling rate you want in K/hr. For example, -200 in the namelist
-!             is a surface cooling rate of -0.2 K/hr
-!
-!=============================================================================
+! WRF:MODEL_LAYER:PHYSICS
 
     MODULE module_diffusion_em
 
     USE module_bc, only: set_physical_bc3d, set_physical_bc2d ! XZ
     USE module_state_description, only: p_m23, p_m13, p_m22, p_m33, p_r23, p_r13, p_r12, p_m12, p_m11, &
-                                        p_mh1, p_mh2, p_mh3, & !ASW
-                                        P_QNS, P_QNR, P_QNG, P_QT, P_QNH, P_QVOLG, & ! XZ
-                                        p_rm11, p_rm22, p_rm33, p_rm12, p_rm13, p_rm23, &
-                                                      p_rh1, p_rh2, p_rh3  ! XS  									
+                                        P_QNS, P_QNR, P_QNG, P_QT, P_QNH, P_QVOLG ! XZ
     USE module_big_step_utilities_em, only: grid_config_rec_type, param_first_scalar, p_qv, p_qi, p_qc
     USE module_model_constants
-    USE module_configure, ONLY : grid_config_rec_type
 
     CONTAINS
 
 !=======================================================================
 !=======================================================================
 
     SUBROUTINE cal_deform_and_div( config_flags, u, v, w, div,       &
                                    defor11, defor22, defor33,        &
                                    defor12, defor13, defor23,        &
                                    nba_rij, n_nba_rij,               &
                                    u_base, v_base, msfux, msfuy,     &
                                    msfvx, msfvy, msftx, msfty,       &
                                    rdx, rdy, dn, dnw, rdz, rdzw,     &
                                    fnm, fnp, cf1, cf2, cf3, zx, zy,  &
                                    ids, ide, jds, jde, kds, kde,     &
                                    ims, ime, jms, jme, kms, kme,     &
                                    its, ite, jts, jte, kts, kte      )
 
 ! History:     Sep 2003  Changes by Jason Knievel and George Bryan, NCAR
 !              Oct 2001  Converted to mass core by Bill Skamarock, NCAR
@@ -2594,41 +2566,41 @@ END SUBROUTINE smag2d_km
     ENDDO
     ENDDO
     ENDDO
  
     DO j = j_start, j_end
     DO i = i_start, i_end
       elt(i,j) = alp1*elt(i,j)/vsc(i,j)
     ENDDO
     ENDDO
 
     hflux: SELECT CASE( config_flags%isfflx )
     CASE (0,2) ! with fixed surface heat flux given in the namelist
      heat_flux = config_flags%tke_heat_flux  ! constant heat flux value
      DO j = j_start, j_end
      DO i = i_start, i_end
         cpm = cp * (1. + 0.8 * moist(i,kts,j,P_QV))
         hfx(i,j)= heat_flux*cpm*rho(i,1,j)
      ENDDO
      ENDDO
  
-    CASE (1,3) ! use surface heat flux computed from surface routine
+    CASE (1) ! use surface heat flux computed from surface routine
      DO j = j_start, j_end
      DO i = i_start, i_end
         cpm = cp * (1. + 0.8 * moist(i,kts,j,P_QV))
         heat_flux = hfx(i,j)/cpm/rho(i,1,j)
      ENDDO
      ENDDO
  
     CASE DEFAULT
      CALL wrf_error_fatal( 'isfflx value invalid for diff_opt=2' )
     END SELECT hflux
 
     DO j = j_start,j_end
     DO i = i_start,i_end
        cpm        = cp  * (1. + 0.8*moist(i,1,j,P_QV))
        sflux(i,j) = (hfx(i,j)/cpm)/rho(i,1,j)
     ENDDO
     ENDDO
 
 !-------Length scale limited by bouyancy effect-----
     gtr  = g/300.
@@ -2882,46 +2854,41 @@ END SUBROUTINE smag2d_km
           elmin(i,k,j) = min(dlu(i,k,j),dld(i,k,j))
           elmin(i,k,j) = elmin(i,k,j)/(1. + (elmin(i,k,j)/2000.))
     ENDDO
     ENDDO
     ENDDO
 
     END SUBROUTINE free_atmos_length
 !=======================================================================
 !=======================================================================
 
 SUBROUTINE horizontal_diffusion_2 ( rt_tendf, ru_tendf, rv_tendf, rw_tendf,    &
                                     tke_tendf,                                 &
                                     moist_tendf, n_moist,                      &
                                     chem_tendf, n_chem,                        &
                                     scalar_tendf, n_scalar,                    &
                                     tracer_tendf, n_tracer,                    &
                                     thp, theta, tke, config_flags,             &
                                     defor11, defor22, defor12,                 &
                                     defor13, defor23,                          &
                                     nba_mij, n_nba_mij,                        &
-                                    rsfs_rmij, n_rsfs_rmij,                    &  ! X
-                                    rsfs_qmij, n_rsfs_qmij,                    &  ! X
-                                    rsfs_qsij, n_rsfs_qsij,                    &  ! X
-                                    u_2, v_2, w_2,                             &  ! Y
-                                    nwtau13, nwtau23,                          & !JDM                                    
-									div,                                       &
+                                    div,                                       &
                                     moist, chem, scalar,tracer,                &
                                     msfux, msfuy, msfvx, msfvy,                &
                                     msftx, msfty, xkmh, xkmv, xkhh, km_opt,    &
                                     rdx, rdy, rdz, rdzw, fnm, fnp,             &
                                     cf1, cf2, cf3, zx, zy, dn, dnw, rho,       &
                                     ids, ide, jds, jde, kds, kde,              &
                                     ims, ime, jms, jme, kms, kme,              &
                                     its, ite, jts, jte, kts, kte               )
 
 !-----------------------------------------------------------------------
 ! Begin declarations.
 
    IMPLICIT NONE
 
    TYPE(grid_config_rec_type), INTENT(IN   ) :: config_flags
 
    INTEGER ,        INTENT(IN   ) ::        ids, ide, jds, jde, kds, kde, &
                                             ims, ime, jms, jme, kms, kme, &
                                             its, ite, jts, jte, kts, kte
 
@@ -2929,43 +2896,40 @@ SUBROUTINE horizontal_diffusion_2 ( rt_tendf, ru_tendf, rv_tendf, rw_tendf,    &
 
    REAL ,           INTENT(IN   ) ::        cf1, cf2, cf3
 
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnm
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnp
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    dnw
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    dn
 
    REAL , DIMENSION( ims:ime, jms:jme) ,         INTENT(IN   ) ::   msfux, &
                                                                     msfuy, &
                                                                     msfvx, &
                                                                     msfvy, &
                                                                     msftx, &
                                                                     msfty
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(INOUT) ::rt_tendf,&
                                                                  ru_tendf,&
                                                                  rv_tendf,&
                                                                  rw_tendf,&
                                                                 tke_tendf
-
-   REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(IN) ::    nwtau13,& 
-                                                                  nwtau23   
   
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme, n_moist),                 &
           INTENT(INOUT) ::                                    moist_tendf
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme, n_chem),                  &
           INTENT(INOUT) ::                                     chem_tendf
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme, n_scalar),                &
           INTENT(INOUT) ::                                   scalar_tendf
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme, n_tracer),                &
           INTENT(INOUT) ::                                   tracer_tendf
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme, n_moist),                 &
           INTENT(IN   ) ::                                          moist
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme, n_chem),                  &
           INTENT(IN   ) ::                                          chem
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme, n_scalar) ,               &
@@ -2973,190 +2937,134 @@ SUBROUTINE horizontal_diffusion_2 ( rt_tendf, ru_tendf, rv_tendf, rw_tendf,    &
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme, n_tracer) ,               &
           INTENT(IN   ) ::                                         tracer
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(IN   ) ::defor11, &
                                                                  defor22, &
                                                                  defor12, &
                                                                  defor13, &
                                                                  defor23, &
                                                                      div, &
                                                                     xkmh, &
                                                                     xkmv, &
                                                                     xkhh, &
                                                                       zx, &
                                                                       zy, &
                                                                    theta, &
                                                                      thp, &
                                                                      tke, &
                                                                      rdz, &
                                                                     rdzw, &
-                                                                     rho, &
-																u_2, v_2, w_2     ! YW
+                                                                     rho
+
 
    REAL ,                                        INTENT(IN   ) ::    rdx, &
                                                                      rdy
-   INTEGER, INTENT(  IN ) :: n_nba_mij, n_rsfs_rmij, n_rsfs_qmij, n_rsfs_qsij   ! XS 
+   INTEGER, INTENT(  IN ) :: n_nba_mij
 
    REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_nba_mij), INTENT(INOUT) &
    :: nba_mij
 
-   ! XS                                                                       
-   REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_rsfs_rmij), INTENT(IN) &     
-   :: rsfs_rmij                                                               
-   REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_rsfs_qmij), INTENT(IN) &     
-   :: rsfs_qmij                                                               
-   REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_rsfs_qsij), INTENT(IN) &     
-   :: rsfs_qsij                                                              
-
 ! LOCAL VARS
 
    INTEGER :: im, ic, is
 
 !  REAL , DIMENSION(its-1:ite+1, kts:kte, jts-1:jte+1)       ::     xkhh
 
 ! End declarations.
 !-----------------------------------------------------------------------
 
 !-----------------------------------------------------------------------
 ! Call diffusion subroutines.
 
     CALL horizontal_diffusion_u_2( ru_tendf, config_flags,                 &
                                    defor11, defor12, div,                  &
                                    nba_mij, n_nba_mij,                     &
-                                   rsfs_rmij, n_rsfs_rmij,                 &   ! XS 
-                                   u_2, v_2,                               &   ! YW 
-								   tke(ims,kms,jms),                       &
+                                   tke(ims,kms,jms),                       &
                                    msfux, msfuy, xkmh, rdx, rdy, fnm, fnp, &
                                    dnw, zx, zy, rdzw, rho,                 &
                                    ids, ide, jds, jde, kds, kde,           &
                                    ims, ime, jms, jme, kms, kme,           &
                                    its, ite, jts, jte, kts, kte           )
 
     CALL horizontal_diffusion_v_2( rv_tendf, config_flags,                 &
                                    defor12, defor22, div,                  &
                                    nba_mij, n_nba_mij,                     &
-                                   rsfs_rmij, n_rsfs_rmij,                 &   ! XS
-								   u_2, v_2,                               &   ! YW
-								   tke(ims,kms,jms),                       &
+                                   tke(ims,kms,jms),                       &
                                    msfvx, msfvy, xkmh, rdx, rdy, fnm, fnp, &
                                    dnw, zx, zy, rdzw, rho,                 &
                                    ids, ide, jds, jde, kds, kde,           &
                                    ims, ime, jms, jme, kms, kme,           &
                                    its, ite, jts, jte, kts, kte           )
 
     CALL horizontal_diffusion_w_2( rw_tendf, config_flags,                 &
                                    defor13, defor23, div,                  &
                                    nba_mij, n_nba_mij,                     &
-                                   rsfs_rmij, n_rsfs_rmij,                 &   ! XS    
-                                   u_2, v_2, w_2,                          &   ! YW    
-                                   nwtau13, nwtau23,                       & !JDM      
                                    tke(ims,kms,jms),                       &
                                    msftx, msfty, xkmv, rdx, rdy, fnm, fnp, &
                                    dn, zx, zy, rdz, rho,                   &
                                    ids, ide, jds, jde, kds, kde,           &
                                    ims, ime, jms, jme, kms, kme,           &
                                    its, ite, jts, jte, kts, kte           )
 
-	! ASW
-	if (config_flags%rsfs_opt.lt.0 .or. config_flags%rsfs_opt.ge.6) THEN   ! no RSFS ASW 
-	
-     CALL horizontal_diffusion_s  ( rt_tendf, config_flags,                 &
-									thp,                                    &
-									msftx, msfty, msfux, msfuy,             &
-									msfvx, msfvy, xkhh, rdx, rdy,           &
-									fnm, fnp, cf1, cf2, cf3,                &
-									zx, zy, rdz, rdzw, dnw, dn, rho,        &
-									.false.,                                &
-									ids, ide, jds, jde, kds, kde,           &
-									ims, ime, jms, jme, kms, kme,           &
-									its, ite, jts, jte, kts, kte           )
-
-	else ! RSFS
-
-	 CALL rsfs_horizontal_diffusion_s( rt_tendf, config_flags, thp,       &   ! XS   
-                                   u_2, v_2,                               &   ! XS   
-                                   rsfs_rmij(ims,kms,jms,P_rh1),           &          
-                                   rsfs_rmij(ims,kms,jms,P_rh2),           &                                             
-								   msftx, msfty, msfux, msfuy,             &
+    CALL horizontal_diffusion_s  ( rt_tendf, config_flags,                 &
+                                   thp,                                    &
+                                   msftx, msfty, msfux, msfuy,             &
                                    msfvx, msfvy, xkhh, rdx, rdy,           &
                                    fnm, fnp, cf1, cf2, cf3,                &
                                    zx, zy, rdz, rdzw, dnw, dn, rho,        &
                                    .false.,                                &
                                    ids, ide, jds, jde, kds, kde,           &
                                    ims, ime, jms, jme, kms, kme,           &
                                    its, ite, jts, jte, kts, kte           )
 
-	end if
-							   
-	IF ( ((km_opt .eq. 2) .and. (.not.config_flags%tke_mix2_off))          & !XZ
+    IF ( ((km_opt .eq. 2) .and. (.not.config_flags%tke_mix2_off))          & !XZ
         .or. (km_opt .eq. 5) )                                             & !XZ
     CALL horizontal_diffusion_s  ( tke_tendf(ims,kms,jms),                 &
                                    config_flags,                           &
                                    tke(ims,kms,jms),                       & 
                                    msftx, msfty, msfux, msfuy,             &
                                    msfvx, msfvy, xkhh, rdx, rdy,           &
                                    fnm, fnp, cf1, cf2, cf3,                &
                                    zx, zy, rdz, rdzw, dnw, dn, rho,        &
                                    .true.,                                 &
                                    ids, ide, jds, jde, kds, kde,           &
                                    ims, ime, jms, jme, kms, kme,           &
                                    its, ite, jts, jte, kts, kte           )
 
     IF ((n_moist .ge. PARAM_FIRST_SCALAR) .and. (.not.config_flags%moist_mix2_off)) THEN
 
       moist_loop: do im = PARAM_FIRST_SCALAR, n_moist
 
-     ! ASW
-     if (config_flags%rsfs_opt.lt.0 .or. config_flags%rsfs_opt.ge.6) THEN   ! no RSFS ASW
-
-           CALL horizontal_diffusion_s( moist_tendf(ims,kms,jms,im),       &
-                                        config_flags,                      &
-                                        moist(ims,kms,jms,im),             &
-                                        msftx, msfty, msfux, msfuy,        &
-                                        msfvx, msfvy, xkhh, rdx, rdy,      &
-                                        fnm, fnp, cf1, cf2, cf3,           &
-                                        zx, zy, rdz, rdzw, dnw, dn, rho,   &
-                                        .false.,                           &
-                                        ids, ide, jds, jde, kds, kde,      &
-                                        ims, ime, jms, jme, kms, kme,      &
-                                        its, ite, jts, jte, kts, kte      )
-
-	 else ! RSFS	 
-          CALL rsfs_horizontal_diffusion_s( moist_tendf(ims,kms,jms,im),       &            
-                                       config_flags,                      &                 
-                                       moist(ims,kms,jms,im),             &                 
-                                       u_2, v_2,                          &                 
-                                       rsfs_qmij(ims,kms,jms,(im-PARAM_FIRST_SCALAR)*3+1), &
-                                       rsfs_qmij(ims,kms,jms,(im-PARAM_FIRST_SCALAR)*3+2), &									   
-									   msftx, msfty, msfux, msfuy,        &
+          CALL horizontal_diffusion_s( moist_tendf(ims,kms,jms,im),       &
+                                       config_flags,                      &
+                                       moist(ims,kms,jms,im),             &
+                                       msftx, msfty, msfux, msfuy,        &
                                        msfvx, msfvy, xkhh, rdx, rdy,      &
                                        fnm, fnp, cf1, cf2, cf3,           &
                                        zx, zy, rdz, rdzw, dnw, dn, rho,   &
                                        .false.,                           &
                                        ids, ide, jds, jde, kds, kde,      &
                                        ims, ime, jms, jme, kms, kme,      &
                                        its, ite, jts, jte, kts, kte      )
 
-	end if
-
-		ENDDO moist_loop
+      ENDDO moist_loop
 
     ENDIF
 
     IF ((n_chem .ge. PARAM_FIRST_SCALAR) .and. (.not.config_flags%chem_mix2_off)) THEN
 
       chem_loop: do ic = PARAM_FIRST_SCALAR, n_chem
 
         CALL horizontal_diffusion_s( chem_tendf(ims,kms,jms,ic),     &
                                      config_flags,                   &
                                      chem(ims,kms,jms,ic),           &
                                      msftx, msfty, msfux, msfuy,     &
                                      msfvx, msfvy, xkhh, rdx, rdy,   &
                                      fnm, fnp, cf1, cf2, cf3,        &
                                      zx, zy, rdz, rdzw, dnw, dn, rho,&
                                      .false.,                        &
                                      ids, ide, jds, jde, kds, kde,   &
                                      ims, ime, jms, jme, kms, kme,   &
                                      its, ite, jts, jte, kts, kte    )
 
       ENDDO chem_loop
@@ -3169,167 +3077,137 @@ SUBROUTINE horizontal_diffusion_2 ( rt_tendf, ru_tendf, rv_tendf, rw_tendf,    &
 
         CALL horizontal_diffusion_s( tracer_tendf(ims,kms,jms,ic),     &
                                      config_flags,                     &
                                      tracer(ims,kms,jms,ic),           &
                                      msftx, msfty, msfux, msfuy,       &
                                      msfvx, msfvy, xkhh, rdx, rdy,     &
                                      fnm, fnp, cf1, cf2, cf3,          &
                                      zx, zy, rdz, rdzw, dnw, dn, rho,  &
                                      .false.,                          &
                                      ids, ide, jds, jde, kds, kde,     &
                                      ims, ime, jms, jme, kms, kme,     &
                                      its, ite, jts, jte, kts, kte     )
 
       ENDDO tracer_loop
 
     ENDIF
     IF ((n_scalar .ge. PARAM_FIRST_SCALAR) .and. (.not.config_flags%scalar_mix2_off)) THEN
 
       scalar_loop: do is = PARAM_FIRST_SCALAR, n_scalar
 
-	! ASW
-    if (config_flags%rsfs_opt.lt.0 .or. config_flags%rsfs_opt.ge.6) THEN   ! no RSFS ASW 
-
-         CALL horizontal_diffusion_s( scalar_tendf(ims,kms,jms,is),     &
-                                      config_flags,                     &
-                                      scalar(ims,kms,jms,is),           &
-                                      msftx, msfty, msfux, msfuy,       &
-                                      msfvx, msfvy, xkhh, rdx, rdy,     &
-                                      fnm, fnp, cf1, cf2, cf3,          &
-                                      zx, zy, rdz, rdzw, dnw, dn, rho,  &
-                                      .false.,                          &
-                                      ids, ide, jds, jde, kds, kde,     &
-                                      ims, ime, jms, jme, kms, kme,     &
-                                      its, ite, jts, jte, kts, kte     )
-	
-	else
-		
-		CALL rsfs_horizontal_diffusion_s( scalar_tendf(ims,kms,jms,is),     &             
-                                     config_flags,                     &                  
-                                     scalar(ims,kms,jms,is),           &                  
-                                     u_2, v_2,                         &                  
-                                     rsfs_qsij(ims,kms,jms,(is-PARAM_FIRST_SCALAR)*3+1),& 
-                                     rsfs_qsij(ims,kms,jms,(is-PARAM_FIRST_SCALAR)*3+2),& 
-									 msftx, msfty, msfux, msfuy,       &
+        CALL horizontal_diffusion_s( scalar_tendf(ims,kms,jms,is),     &
+                                     config_flags,                     &
+                                     scalar(ims,kms,jms,is),           &
+                                     msftx, msfty, msfux, msfuy,       &
                                      msfvx, msfvy, xkhh, rdx, rdy,     &
                                      fnm, fnp, cf1, cf2, cf3,          &
                                      zx, zy, rdz, rdzw, dnw, dn, rho,  &
                                      .false.,                          &
                                      ids, ide, jds, jde, kds, kde,     &
                                      ims, ime, jms, jme, kms, kme,     &
                                      its, ite, jts, jte, kts, kte     )
 
-	end if
-
       ENDDO scalar_loop
 
     ENDIF
 
     END SUBROUTINE horizontal_diffusion_2
 
 !=======================================================================
 !=======================================================================
 
 SUBROUTINE horizontal_diffusion_u_2( tendency, config_flags,              &
                                      defor11, defor12, div,               &
                                      nba_mij, n_nba_mij,                  &
-                                     rsfs_rmij, n_rsfs_rmij,              &   ! XS 
-                                     u_2, v_2,                            &   ! YW                                      
-									 tke,                                 &
+                                     tke,                                 &
                                      msfux, msfuy,                        &
                                      xkmh, rdx, rdy, fnm, fnp,            &
                                      dnw, zx, zy, rdzw, rho,              &
                                      ids, ide, jds, jde, kds, kde,        &
                                      ims, ime, jms, jme, kms, kme,        &
                                      its, ite, jts, jte, kts, kte        )
 
 !-----------------------------------------------------------------------
 ! Begin declarations.
 
    IMPLICIT NONE
 
    TYPE(grid_config_rec_type), INTENT(IN   ) :: config_flags
 
    INTEGER ,        INTENT(IN   ) ::        ids, ide, jds, jde, kds, kde, &
                                             ims, ime, jms, jme, kms, kme, &
                                             its, ite, jts, jte, kts, kte
 
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnm
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnp
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    dnw
 
    REAL , DIMENSION( ims:ime, jms:jme) ,         INTENT(IN   ) ::  msfux, &
                                                                    msfuy
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(INOUT) ::tendency
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(IN   ) ::   rdzw, &
                                                                      rho
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(IN   ) ::defor11, &
                                                                  defor12, &
                                                                      div, &
                                                                      tke, &
                                                                     xkmh, &
                                                                       zx, &
-                                                                      zy, &
-																	 u_2, v_2    ! YW
+                                                                      zy
 
-   INTEGER, INTENT(  IN ) :: n_nba_mij, &
-	                         n_rsfs_rmij   ! XS 
+   INTEGER, INTENT(  IN ) :: n_nba_mij
 
    REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_nba_mij),  INTENT(INOUT) &
    :: nba_mij
 
-   REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_rsfs_rmij),  INTENT(IN) &
-   :: rsfs_rmij   ! XS                                                    
-
    REAL ,                                        INTENT(IN   ) ::    rdx, &
                                                                      rdy
 ! Local data
 
    INTEGER :: i, j, k, ktf
 
    INTEGER :: i_start, i_end, j_start, j_end
    INTEGER :: is_ext,ie_ext,js_ext,je_ext
 
    REAL , DIMENSION( its-1:ite+1, kts:kte, jts-1:jte+1)    :: titau1avg, &
                                                               titau2avg, &
                                                                  titau1, &
                                                                  titau2, &
                                                                  xkxavg, &
                                                                   rravg, &
                                                                 zy_at_u, &
                                                                 zx_at_u
 
 ! new
 !                                                                 zxavg, &
 !                                                                 zyavg
    REAL :: mrdx, mrdy, rcoup
 
    REAL :: tmpzy, tmpzeta_z
 
    REAL :: tmpdz
 
    REAL :: term1, term2, term3
 
-   REAL :: du, rr, cr, ubar, vbar
 ! End declarations.
 !-----------------------------------------------------------------------
 
    ktf=MIN(kte,kde-1)
 
 !-----------------------------------------------------------------------
 ! u :   p (.), u(|), w(-)
 !
 !       p  u  p  u                                  u     u
 !
 ! p  |  .  |  .  |  .  |   k+1                |  .  |  .  |  .  |   k+1
 !
 ! w     - 13  -     -      k+1                     13               k+1
 !
 ! p  |  11 O 11  |  .  |   k                  |  12 O 12  |  .  |   k
 !
 ! w     - 13  -     -      k                       13               k
 !
 ! p  |  .  |  .  |  .  |   k-1                |  .  |  .  |  .  |   k-1
 !
@@ -3343,139 +3221,58 @@ SUBROUTINE horizontal_diffusion_u_2( tendency, config_flags,              &
 
    IF ( config_flags%open_xs .or. config_flags%specified .or. &
         config_flags%nested) i_start = MAX(ids+1,its)
    IF ( config_flags%open_xe .or. config_flags%specified .or. &
         config_flags%nested) i_end   = MIN(ide-1,ite)
    IF ( config_flags%open_ys .or. config_flags%specified .or. &
         config_flags%nested) j_start = MAX(jds+1,jts)
    IF ( config_flags%open_ye .or. config_flags%specified .or. &
         config_flags%nested) j_end   = MIN(jde-2,jte)
       IF ( config_flags%periodic_x ) i_start = its
       IF ( config_flags%periodic_x ) i_end = ite
 
 ! titau1 = titau11
    is_ext=1
    ie_ext=0
    js_ext=0
    je_ext=0
    CALL cal_titau_11_22_33( config_flags, titau1,            &
                             tke, xkmh, defor11,              &
                             nba_mij(ims,kms,jms,P_m11), rho, &
-							rsfs_rmij(ims,kms,jms,P_rm11),   &   ! XS
                             is_ext, ie_ext, js_ext, je_ext,  &
                             ids, ide, jds, jde, kds, kde,    &
                             ims, ime, jms, jme, kms, kme,    &
                             its, ite, jts, jte, kts, kte     )
-   
-   !ASW
-   if (config_flags%dwlsurfsmooth_opt .eq. 1) then
-
-   ! YW and XS                                                          
-   do j = j_start-1, j_end+1                                            
-      do k = kts, ktf                                                   
-         do i = i_start-1, i_end+1                                      
-            ubar = 0.5 * (u_2(i, k, j) + u_2(i+1, k, j))                
-            du = u_2(i+1, k, j) - u_2(i, k, j)                          
-            if (abs(du) .lt. 1.0e-6) then                               
-               if (du .ge. 0.0) then                                    
-                  du = 1.0e-6                                           
-               else                                                     
-                  du = -1.0e-6                                          
-               end if                                                   
-            end if                                                      
-                                                                        
-            if (ubar .ge. 0) then                                       
-               rr = (u_2(i,k,j) - u_2(i-1,k,j)) / du                    
-            else                                                        
-               rr = (u_2(i+2,k,j) - u_2(i+1,k,j)) / du                  
-            end if                                                      
-                                                                        
-            cr = max(0.0, min(1.0, rr))                                 
-                                                                        
-            titau1(i, k, j) = titau1(i, k, j) * cr                      
-                                                                        
-            if ((k-kts).lt.4) then                                      
-              titau1(i, k, j) = 0.0                                     
-            else if ((k-kts)>3 .and. (k-kts)<7) then                    
-              titau1(i, k, j) = titau1(i, k, j) * (real(k)-3.0)/4.0     
-            end if                                                      
-                                                                        
-         end do                                                         
-      end do                                                            
-   end do                                                               
-                                                                        
-   end if ! ASW                                                    
 
 ! titau2 = titau12
    is_ext=0
    ie_ext=0
    js_ext=0
    je_ext=1
    CALL cal_titau_12_21( config_flags, titau2,            &
                          xkmh, defor12,                   &
                          nba_mij(ims,kms,jms,P_m12), rho, &
-						 rsfs_rmij(ims,kms,jms,P_rm12),   &   ! XS
                          is_ext, ie_ext, js_ext, je_ext,  &
                          ids, ide, jds, jde, kds, kde,    &
                          ims, ime, jms, jme, kms, kme,    &
                          its, ite, jts, jte, kts, kte     )
 
-   !ASW
-   if (config_flags%dwlsurfsmooth_opt .eq. 1) then
-
-   ! YW and XS                                                       
-   do j = j_start-1, j_end+1                                         
-      do k = kts, ktf                                                
-          do i = i_start-1, i_end+1                                  
-            vbar = 0.5 * (v_2(i, k, j) + v_2(i-1, k, j))             
-            du = u_2(i, k, j) - u_2(i, k, j-1)                       
-            if (abs(du) .lt. 1.0e-6) then                            
-                if (du .ge. 0.0) then                                
-                  du = 1.0e-6                                        
-                else                                                 
-                  du = -1.0e-6                                       
-                end if                                               
-            end if                                                   
-                                                                     
-            if (vbar .ge. 0) then                                    
-                rr = (u_2(i,k,j-1) - u_2(i,k,j-2)) / du              
-            else                                                     
-                rr = (u_2(i,k,j+1) - u_2(i,k,j)) / du                
-            end if                                                   
-                                                                     
-            cr = max(0.0, min(1.0, rr))                              
-                                                                     
-            titau2(i, k, j) = titau2(i, k, j) * cr                   
-                                                                     
-            if ((k-kts).lt.4) then                                   
-              titau2(i, k, j) = 0.0                                  
-            else if ((k-kts)>3 .and. (k-kts)<7) then                 
-              titau2(i, k, j) = titau2(i, k, j) * (real(k)-3.0)/4.0  
-            end if                                                   
-                                                                     
-          end do                                                     
-      end do                                                         
-    end do       
-
-    end if ! ASW                                                    
-                                                                     
-                                                                   
 ! titau1avg = titau11avg
 ! titau2avg = titau12avg
 
    DO j = j_start, j_end
    DO k = kts+1,ktf
    DO i = i_start, i_end
       titau1avg(i,k,j)=0.5*(fnm(k)*(titau1(i-1,k  ,j)+titau1(i,k  ,j))+ &
                             fnp(k)*(titau1(i-1,k-1,j)+titau1(i,k-1,j)))
       titau2avg(i,k,j)=0.5*(fnm(k)*(titau2(i,k  ,j+1)+titau2(i,k  ,j))+ &
                             fnp(k)*(titau2(i,k-1,j+1)+titau2(i,k-1,j)))
 !     tmpzy = 0.25*( zy(i-1,k,j  )+zy(i,k,j  )+ &
 !                    zy(i-1,k,j+1)+zy(i,k,j+1)  )
 !      tmpzeta_z = 0.5*(zeta_z(i,j)+zeta_z(i-1,j))
 !      titau1avg(i,k,j)=titau1avg(i,k,j)*zx(i,k,j)*tmpzeta_z
 !      titau2avg(i,k,j)=titau2avg(i,k,j)*tmpzy    *tmpzeta_z
 
       zx_at_u(i, k, j) = 0.5 * (zx(i, k, j) + zx(i, k + 1 , j))
       zy_at_u(i, k, j) = 0.125 * (zy(i - 1, k, j) + zy(i, k, j) + &
           zy(i - 1, k, j + 1) + zy(i, k, j + 1) + zy(i - 1, k + 1, j) + &
           zy(i, k + 1, j) + zy(i - 1, k + 1, j + 1) + zy(i, k + 1, j + 1))
@@ -3509,118 +3306,109 @@ SUBROUTINE horizontal_diffusion_u_2( tendency, config_flags,              &
 
       tmpdz = (1./rdzw(i,k,j)+1./rdzw(i-1,k,j))/2.
       tendency(i,k,j)=tendency(i,k,j) +  g*tmpdz/dnw(k) *             &
            (mrdx*(titau1(i,k,j  ) - titau1(i-1,k,j)) +                &
             mrdy*(titau2(i,k,j+1) - titau2(i  ,k,j)) -                &
             msfux(i,j)*zx_at_u(i,k,j)*(titau1avg(i,k+1,j)-titau1avg(i,k,j)) / tmpdz - &
             msfuy(i,j)*zy_at_u(i,k,j)*(titau2avg(i,k+1,j)-titau2avg(i,k,j)) / tmpdz   &
                                                                   )
    ENDDO
    ENDDO
    ENDDO
 
 END SUBROUTINE horizontal_diffusion_u_2
 
 !=======================================================================
 !=======================================================================
 
 SUBROUTINE horizontal_diffusion_v_2( tendency, config_flags,              &
                                      defor12, defor22, div,               &           
                                      nba_mij, n_nba_mij,                  &
-                                     rsfs_rmij, n_rsfs_rmij,              &   ! XS
-									 u_2, v_2,                            &   ! YW
-									 tke,                                 &
+                                     tke,                                 &
                                      msfvx, msfvy,                        &
                                      xkmh, rdx, rdy, fnm, fnp,            &
                                      dnw, zx, zy, rdzw, rho,              &
                                      ids, ide, jds, jde, kds, kde,        &
                                      ims, ime, jms, jme, kms, kme,        &
                                      its, ite, jts, jte, kts, kte        )
 
 !-----------------------------------------------------------------------
 ! Begin declarations.
 
    IMPLICIT NONE
 
    TYPE(grid_config_rec_type), INTENT(IN   ) :: config_flags
 
    INTEGER ,        INTENT(IN   ) ::        ids, ide, jds, jde, kds, kde, &
                                             ims, ime, jms, jme, kms, kme, &
                                             its, ite, jts, jte, kts, kte
 
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnm
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnp
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    dnw
 
    REAL , DIMENSION( ims:ime, jms:jme) ,         INTENT(IN   ) ::  msfvx, &
                                                                    msfvy
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(INOUT) :: tendency
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(IN   ) ::defor12, &
                                                                  defor22, &
                                                                      div, &
                                                                      tke, &
                                                                     xkmh, &
                                                                       zx, &
                                                                       zy, &
                                                                     rdzw, &
-                                                                     rho, &
-																 u_2, v_2    ! YW 	 
+                                                                     rho
 
-   INTEGER,  INTENT(  IN ) :: n_nba_mij, &
-							  n_rsfs_rmij   ! XS 
+   INTEGER,  INTENT(  IN ) :: n_nba_mij
 
    REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_nba_mij),  INTENT(INOUT) &
    :: nba_mij
 
-   REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_rsfs_rmij),  INTENT(IN) &   
-   :: rsfs_rmij  ! XS                                                        
-
    REAL ,                                        INTENT(IN   ) ::    rdx, &
                                                                      rdy
 
 ! Local data
 
    INTEGER :: i, j, k, ktf
 
    INTEGER :: i_start, i_end, j_start, j_end
    INTEGER :: is_ext,ie_ext,js_ext,je_ext
 
    REAL , DIMENSION( its-1:ite+1, kts:kte, jts-1:jte+1)    :: titau1avg, &
                                                               titau2avg, &
                                                                  titau1, &
                                                                  titau2, &
                                                                  xkxavg, &
                                                                   rravg, &
                                                                 zy_at_v, &
                                                                 zx_at_v
 ! new
 !                                                                 zxavg, &
 !                                                                 zyavg
 
    REAL :: mrdx, mrdy, rcoup
    REAL :: tmpdz
    REAL :: tmpzx, tmpzeta_z
 
-   REAL :: dv, rr, cr, ubar, vbar
-
 ! End declarations.
 !-----------------------------------------------------------------------
 
    ktf=MIN(kte,kde-1)
 
 !-----------------------------------------------------------------------
 ! v :   p (.), v(+), w(-)
 !
 !       p  v  p  v                                  v     v
 !
 ! p  +  .  +  .  +  .  +   k+1                +  .  +  .  +  .  +   k+1
 !
 ! w     - 23  -     -      k+1                     23               k+1
 !
 ! p  +  22 O 22  +  .  +   k                  +  21 O 21  +  .  +   k
 !
 ! w     - 23  -     -      k                       23               k
 !
 ! p  +  .  +  .  +  .  +   k-1                +  .  +  .  +  .  +   k-1
 !
@@ -3634,139 +3422,58 @@ SUBROUTINE horizontal_diffusion_v_2( tendency, config_flags,              &
 
    IF ( config_flags%open_xs .or. config_flags%specified .or. &
         config_flags%nested) i_start = MAX(ids+1,its)
    IF ( config_flags%open_xe .or. config_flags%specified .or. &
         config_flags%nested) i_end   = MIN(ide-2,ite)
    IF ( config_flags%open_ys .or. config_flags%specified .or. &
         config_flags%nested) j_start = MAX(jds+1,jts)
    IF ( config_flags%open_ye .or. config_flags%specified .or. &
         config_flags%nested) j_end   = MIN(jde-1,jte)
       IF ( config_flags%periodic_x ) i_start = its
       IF ( config_flags%periodic_x ) i_end = MIN(ite,ide-1)
 
 ! titau1 = titau21
    is_ext=0
    ie_ext=1
    js_ext=0
    je_ext=0
    CALL cal_titau_12_21( config_flags, titau1,           &
                          xkmh, defor12,                  &
                          nba_mij(ims,kms,jms,P_m12),rho, &
-						 rsfs_rmij(ims,kms,jms,P_rm12),  &   ! XS
                          is_ext,ie_ext,js_ext,je_ext,    &
                          ids, ide, jds, jde, kds, kde,   &
                          ims, ime, jms, jme, kms, kme,   &
                          its, ite, jts, jte, kts, kte    )
-                                                                      
-   !ASW
-   if (config_flags%dwlsurfsmooth_opt .eq. 1) then
-
-   ! YW and XS                                                        
-  do j = j_start-1, j_end+1                                           
-    do k = kts, ktf                                                   
-        do i = i_start-1, i_end+1                                     
-          ubar = 0.5 * (u_2(i, k, j) + u_2(i, k, j-1))                
-          dv = v_2(i, k, j) - v_2(i-1, k, j)                          
-          if (abs(dv) .lt. 1.0e-6) then                               
-              if (dv .ge. 0.0) then                                   
-                dv = 1.0e-6                                           
-              else                                                    
-                dv = -1.0e-6                                          
-              end if                                                  
-          end if                                                      
-                                                                      
-          if (ubar .ge. 0) then                                       
-              rr = (v_2(i-1,k,j) - v_2(i-2,k,j)) / dv                 
-          else                                                        
-              rr = (v_2(i+1,k,j) - v_2(i,k,j)) / dv                   
-          end if                                                      
-                                                                      
-          cr = max(0.0, min(1.0, rr))                                 
-                                                                      
-          titau1(i, k, j) = titau1(i, k, j) * cr                      
-                                                                      
-          if ((k-kts).lt.4) then                                      
-            titau1(i, k, j) = 0.0                                     
-          else if ((k-kts)>3 .and. (k-kts)<7) then                    
-            titau1(i, k, j) = titau1(i, k, j) * (real(k)-3.0)/4.0     
-          end if                                                      
-                                                                      
-        end do                                                        
-    end do                                                            
-  end do    
-
-  end if !ASW                                                          
-                                                                      
-                                                                      
+
 ! titau2 = titau22
    is_ext=0
    ie_ext=0
    js_ext=1
    je_ext=0
    CALL cal_titau_11_22_33( config_flags, titau2,           &
                             tke, xkmh, defor22,             &
                             nba_mij(ims,kms,jms,P_m22),rho, &
-							rsfs_rmij(ims,kms,jms,P_rm22),   &   ! XS
                             is_ext, ie_ext, js_ext, je_ext, &
                             ids, ide, jds, jde, kds, kde,   &
                             ims, ime, jms, jme, kms, kme,   &
                             its, ite, jts, jte, kts, kte    )
 
-   !ASW
-   if (config_flags%dwlsurfsmooth_opt .eq. 1) then
-
-   ! YW and XS                                                       
-   do j = j_start-1, j_end+1                                         
-      do k = kts, ktf                                                
-         do i = i_start-1, i_end+1                                   
-            vbar = 0.5 * (v_2(i, k, j) + v_2(i, k, j+1))             
-            dv = v_2(i, k, j+1) - v_2(i, k, j)                       
-            if (abs(dv) .lt. 1.0e-6) then                            
-                if (dv .ge. 0.0) then                                
-                  dv = 1.0e-6                                        
-                else                                                 
-                  dv = -1.0e-6                                       
-                end if                                               
-            end if                                                   
-            if (vbar .ge. 0) then                                    
-                rr = (v_2(i,k,j) - v_2(i,k,j-1)) / dv                
-            else                                                     
-                rr = (v_2(i,k,j+2) - v_2(i,k,j+1)) / dv              
-            end if                                                   
-                                                                     
-            cr = max(0.0, min(1.0, rr))                              
-                                                                     
-            titau2(i, k, j) = titau2(i, k, j) * cr                   
-                                                                     
-            if ((k-kts).lt.4) then                                   
-              titau2(i, k, j) = 0.0                                  
-            else if ((k-kts)>3 .and. (k-kts)<7) then                 
-              titau2(i, k, j) = titau2(i, k, j) * (real(k)-3.0)/4.0  
-            end if                                                   
-                                                                     
-            end do                                                   
-        end do                                                       
-     end do  
-
-     end if !ASW                                                        
-                                                                     
-
    DO j = j_start, j_end
    DO k = kts+1,ktf
    DO i = i_start, i_end
       titau1avg(i,k,j)=0.5*(fnm(k)*(titau1(i+1,k  ,j)+titau1(i,k  ,j))+ &
                             fnp(k)*(titau1(i+1,k-1,j)+titau1(i,k-1,j)))
       titau2avg(i,k,j)=0.5*(fnm(k)*(titau2(i,k  ,j-1)+titau2(i,k  ,j))+ &
                             fnp(k)*(titau2(i,k-1,j-1)+titau2(i,k-1,j)))
 
 !      tmpzx = 0.25*( zx(i,k,j  )+zx(i+1,k,j  )+ &
 !                     zx(i,k,j-1)+zx(i+1,k,j-1)  )
       zx_at_v(i, k, j) = 0.125 * (zx(i, k, j) + zx(i + 1, k, j) +     &
           zx(i, k, j - 1) + zx(i + 1, k, j - 1) + zx(i, k + 1, j) +   &
           zx(i + 1, k + 1, j) + zx(i, k + 1, j - 1) + zx(i + 1, k + 1, j - 1))
       zy_at_v(i, k, j) = 0.5 * (zy(i, k, j) + zy(i, k + 1 , j))
 
 !     titau1avg(i,k,j)=titau1avg(i,k,j)*tmpzx
 !     titau2avg(i,k,j)=titau2avg(i,k,j)*zy(i,k,j)
 
 
    ENDDO
@@ -3795,122 +3502,108 @@ SUBROUTINE horizontal_diffusion_v_2( tendency, config_flags,              &
       tmpdz = (1./rdzw(i,k,j)+1./rdzw(i,k,j-1))/2.
       tendency(i,k,j)=tendency(i,k,j) +    g*tmpdz/dnw(k) *           &
            (mrdy*(titau2(i,k,j  ) - titau2(i,k,j-1)) +                &
             mrdx*(titau1(i+1,k,j) - titau1(i  ,k,j)) -                &
             msfvx(i,j)*zx_at_v(i,k,j)*(titau1avg(i,k+1,j)-titau1avg(i,k,j)) / tmpdz - &
             msfvy(i,j)*zy_at_v(i,k,j)*(titau2avg(i,k+1,j)-titau2avg(i,k,j)) / tmpdz   &
                                                                   )
 
    ENDDO
    ENDDO
    ENDDO
 
 END SUBROUTINE horizontal_diffusion_v_2
 
 !=======================================================================
 !=======================================================================
 
 SUBROUTINE horizontal_diffusion_w_2( tendency, config_flags,              &
                                      defor13, defor23, div,               &
                                      nba_mij, n_nba_mij,                  &
-                                     rsfs_rmij, n_rsfs_rmij,              &   ! XS            
-                                     u_2, v_2, w_2,                       &   ! YW and XS     
-                                     nwtau13, nwtau23,                    & !JDM              									 
-									 tke,                                 &
+                                     tke,                                 &
                                      msftx, msfty,                        &
                                      xkmv, rdx, rdy, fnm, fnp,            &
                                      dn, zx, zy, rdz, rho,                &
                                      ids, ide, jds, jde, kds, kde,        &
                                      ims, ime, jms, jme, kms, kme,        &
                                      its, ite, jts, jte, kts, kte        )
 
 !-----------------------------------------------------------------------
 ! Begin declarations.
 
    IMPLICIT NONE
 
    TYPE(grid_config_rec_type), INTENT(IN   ) :: config_flags
 
    INTEGER ,        INTENT(IN   ) ::        ids, ide, jds, jde, kds, kde, &
                                             ims, ime, jms, jme, kms, kme, &
                                             its, ite, jts, jte, kts, kte
 
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnm
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnp
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    dn
 
    REAL , DIMENSION( ims:ime, jms:jme) ,         INTENT(IN   ) ::  msftx, &
                                                                    msfty
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(INOUT) :: tendency
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(IN   ) ::defor13, &
                                                                  defor23, &
                                                                      div, &
                                                                      tke, &
                                                                     xkmv, &
                                                                       zx, &
                                                                       zy, &
                                                                      rdz, &
-                                                                     rho, &
-																 u_2, v_2, w_2   ! YW and XS
-
-   INTEGER,  INTENT(  IN ) :: n_nba_mij, &
-							  n_rsfs_rmij   ! XS 
+                                                                     rho
 
-   REAL , DIMENSION(ims:ime, kms:kme, jms:jme), INTENT( IN ) &   !JDM      
-   :: nwtau13, nwtau23                                                     
+   INTEGER,  INTENT(  IN ) :: n_nba_mij
 
    REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_nba_mij),  INTENT(INOUT) &
    :: nba_mij
 
-   REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_rsfs_rmij),  INTENT(IN) &    
-   :: rsfs_rmij   ! XS                                                        
-
    REAL ,                                        INTENT(IN   ) ::    rdx, &
                                                                      rdy
 
 ! Local data
 
    INTEGER :: i, j, k, ktf
 
    INTEGER :: i_start, i_end, j_start, j_end
    INTEGER :: is_ext,ie_ext,js_ext,je_ext
 
    REAL , DIMENSION( its-1:ite+1, kts:kte, jts-1:jte+1)    :: titau1avg, &
                                                               titau2avg, &
                                                                  titau1, &
                                                                  titau2, &
                                                                  xkxavg, &
                                                                   rravg, &
                                                                 zx_at_w, &
                                                                 zy_at_w
 ! new
 !                                                                 zxavg, &
 !                                                                 zyavg
 
    REAL :: mrdx, mrdy, rcoup
 
    REAL :: tmpzx, tmpzy, tmpzeta_z
-
-   real :: ubar, vbar, dw, rr, cr   ! YW 
-
 ! 
 ! End declarations.
 !-----------------------------------------------------------------------
 
    ktf=MIN(kte,kde-1)
 
 !-----------------------------------------------------------------------
 ! w :   p (.), u(|), v(+), w(-)
 !
 !       p  u  p  u                               p  v  p  v
 !
 ! w     -     -     -      k+1             w     -     -     -      k+1
 !
 ! p     .  | 33  |  .      k               p     .  + 33  +  .      k
 !
 ! w     -  31 O 31  -      k               w     -  32 O 32  -      k
 !
 ! p     .  | 33  |  .      k-1             p     .  | 33  |  .      k-1
 !
 ! w     -     -     -      k-1             w     -     -     -      k-1
@@ -3923,143 +3616,59 @@ SUBROUTINE horizontal_diffusion_w_2( tendency, config_flags,              &
    j_end   = MIN(jte,jde-1)
 
    IF ( config_flags%open_xs .or. config_flags%specified .or. &
         config_flags%nested) i_start = MAX(ids+1,its)
    IF ( config_flags%open_xe .or. config_flags%specified .or. &
         config_flags%nested) i_end   = MIN(ide-2,ite)
    IF ( config_flags%open_ys .or. config_flags%specified .or. &
         config_flags%nested) j_start = MAX(jds+1,jts)
    IF ( config_flags%open_ye .or. config_flags%specified .or. &
         config_flags%nested) j_end   = MIN(jde-2,jte)
       IF ( config_flags%periodic_x ) i_start = its
       IF ( config_flags%periodic_x ) i_end = MIN(ite,ide-1)
 
 ! titau1 = titau31
    is_ext=0
    ie_ext=1
    js_ext=0
    je_ext=0
    CALL cal_titau_13_31( config_flags, titau1, defor13,   &
                          nba_mij(ims,kms,jms,P_m13),      &
-						 rsfs_rmij(ims,kms,jms,P_rm13),   &   ! XS
-						 nwtau13,                         & !JDM
                          xkmv, fnm, fnp, rho,             &
                          is_ext, ie_ext, js_ext, je_ext,  &
                          ids, ide, jds, jde, kds, kde,    &
                          ims, ime, jms, jme, kms, kme,    &
                          its, ite, jts, jte, kts, kte     )
 
-   !ASW
-   if (config_flags%dwlsurfsmooth_opt .eq. 1) then
-                                                                    
-   ! YW and XS                                                      
-   do j = j_start-1, j_end+1                                        
-      do k = kts+1, ktf                                             
-          do i = i_start-1, i_end+1                                 
-            ubar = 0.5 * (u_2(i, k, j) + u_2(i, k-1, j))            
-            dw = w_2(i, k, j) - w_2(i-1, k, j)                      
-            if (abs(dw) .lt. 1.0e-8) then                           
-                if (dw .ge. 0.0) then                               
-                  dw = 1.0e-8                                       
-                else                                                
-                  dw = -1.0e-8                                      
-                end if                                              
-            end if                                                  
-                                                                    
-            if (ubar .ge. 0) then                                   
-                rr = (w_2(i-1,k,j) - w_2(i-2,k,j)) / dw             
-            else                                                    
-                rr = (w_2(i+1,k,j) - w_2(i,k,j)) / dw               
-            end if                                                  
-                                                                    
-            cr = max(0.0, min(1.0, rr))                             
-                                                                    
-            titau1(i, k, j) = titau1(i, k, j) * cr                  
-                                                                    
-            if ((k-kts).lt.4) then                                  
-              titau1(i, k, j) = 0.0                                 
-            else if ((k-kts)>3 .and. (k-kts)<7) then                
-              titau1(i, k, j) = titau1(i, k, j) * (real(k)-3.0)/4.0 
-            end if                                                  
-                                                                    
-          end do                                                    
-      end do                                                        
-    end do
-
-    end if !ASW                                                          
-                                                                    
-
 ! titau2 = titau32
    is_ext=0
    ie_ext=0
    js_ext=0
    je_ext=1
    CALL cal_titau_23_32( config_flags, titau2, defor23,   &
                          nba_mij(ims,kms,jms,P_m23),      &
-					     rsfs_rmij(ims,kms,jms,P_rm23),   &   ! XS
-					     nwtau23,                         & !JDM   
                          xkmv, fnm, fnp, rho,             &
                          is_ext, ie_ext, js_ext, je_ext,  &
                          ids, ide, jds, jde, kds, kde,    &
                          ims, ime, jms, jme, kms, kme,    &
                          its, ite, jts, jte, kts, kte     )
 
-   !ASW
-   if (config_flags%dwlsurfsmooth_opt .eq. 1) then
-
-    ! YW and XS                                                        
-    do j = j_start-1, j_end+1                                          
-       do k = kts+1, ktf                                               
-           do i = i_start-1, i_end+1                                   
-             vbar = 0.5 * (v_2(i, k, j) + v_2(i, k-1, j))              
-             dw = w_2(i, k, j) - w_2(i, k, j-1)                        
-             if (abs(dw) .lt. 1.0e-8) then                             
-                 if (dw .ge. 0.0) then                                 
-                   dw = 1.0e-8                                         
-                 else                                                  
-                   dw = -1.0e-8                                        
-                 end if                                                
-             end if                                                    
-                                                                       
-             if (vbar .ge. 0) then                                     
-                 rr = (w_2(i,k,j-1) - w_2(i,k,j-2)) / dw               
-             else                                                      
-                 rr = (w_2(i,k,j+1) - w_2(i,k,j)) / dw                 
-             end if                                                    
-                                                                       
-             cr = max(0.0, min(1.0, rr))                               
-                                                                       
-             titau2(i, k, j) = titau2(i, k, j) * cr                    
-                                                                       
-             if ((k-kts).lt.4) then                                    
-               titau2(i, k, j) = 0.0                                   
-             else if ((k-kts)>3 .and. (k-kts)<7) then                  
-               titau2(i, k, j) = titau2(i, k, j) * (real(k)-3.0)/4.0   
-             end if                                                    
-                                                                       
-           end do                                                      
-       end do                                                          
-     end do       
-
-     end if !ASW                                                     
-                                                                       
-                                                                       
 ! titau1avg = titau31avg * zx * zeta_z = titau13avg * zx * zeta_z
 ! titau2avg = titau32avg * zy * zeta_z = titau23avg * zy * zeta_z
 
    DO j = j_start, j_end
    DO k = kts,ktf
    DO i = i_start, i_end
       titau1avg(i,k,j)=0.25*(titau1(i+1,k+1,j)+titau1(i,k+1,j)+ &
                              titau1(i+1,k  ,j)+titau1(i,k  ,j))
       titau2avg(i,k,j)=0.25*(titau2(i,k+1,j+1)+titau2(i,k+1,j)+ &
                              titau2(i,k  ,j+1)+titau2(i,k  ,j))
       zx_at_w(i, k, j) = 0.5 * (zx(i, k, j) + zx(i + 1, k, j))
       zy_at_w(i, k, j) = 0.5 * (zy(i, k, j) + zy(i, k, j + 1))
 ! new
 !      tmpzx  =0.25*( zx(i,k  ,j)+zx(i+1,k  ,j)+ &
 !                     zx(i,k+1,j)+zx(i+1,k+1,j)  )
 !      tmpzy  =0.25*( zy(i,k  ,j)+zy(i,k  ,j+1)+ &
 !                     zy(i,k+1,j)+zy(i,k+1,j+1)  )
 
 !      titau1avg(i,k,j)=titau1avg(i,k,j)*tmpzx
 !      titau2avg(i,k,j)=titau2avg(i,k,j)*tmpzy
@@ -4372,586 +3981,131 @@ SUBROUTINE horizontal_diffusion_s (tendency, config_flags,                &
             msftx(i,j)*zx_at_m(i, k, j)*(H1avg(i,k+1,j)-H1avg(i,k,j))*rdzw(i,k,j) - &
             msfty(i,j)*zy_at_m(i, k, j)*(H2avg(i,k+1,j)-H2avg(i,k,j))*rdzw(i,k,j)   &
            )
    ENDDO
    ENDDO
    ENDDO
 
    IF ( doing_tke ) THEN
       DO j = j_start, j_end
       DO k = kts,ktf
       DO i = i_start, i_end
           tendency(i,k,j)=tmptendf(i,k,j)+2.* &
                           (tendency(i,k,j)-tmptendf(i,k,j))
       ENDDO
       ENDDO
       ENDDO
    ENDIF
 
 END SUBROUTINE horizontal_diffusion_s
 
-!=======================================================================
-!=======================================================================
-!  XS
-
-
-SUBROUTINE rsfs_horizontal_diffusion_s(tendency, config_flags, var,          &
-                                      u_2, v_2,                              &  ! XS
-                                      rsfs_h1, rsfs_h2,                      &  ! XS
-                                      msftx, msfty, msfux, msfuy,            &
-                                      msfvx, msfvy, xkhh, rdx, rdy,          &
-                                      fnm, fnp, cf1, cf2, cf3,               &
-                                      zx, zy, rdz, rdzw, dnw, dn, rho,       &
-                                      doing_tke,                             &
-                                      ids, ide, jds, jde, kds, kde,          &
-                                      ims, ime, jms, jme, kms, kme,          &
-                                      its, ite, jts, jte, kts, kte           )
-
-!-----------------------------------------------------------------------
-! Begin declarations.
-
-IMPLICIT NONE
-
-TYPE(grid_config_rec_type), INTENT(IN   ) :: config_flags
-
-INTEGER ,        INTENT(IN   ) ::        ids, ide, jds, jde, kds, kde, &
-           ims, ime, jms, jme, kms, kme, &
-           its, ite, jts, jte, kts, kte
-
-LOGICAL,         INTENT(IN   ) ::        doing_tke
-
-REAL , INTENT(IN   )           ::        cf1, cf2, cf3
-
-REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnm
-REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnp
-REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::     dn
-REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    dnw
-
-REAL , DIMENSION( ims:ime, jms:jme) ,         INTENT(IN   ) ::   msfux
-REAL , DIMENSION( ims:ime, jms:jme) ,         INTENT(IN   ) ::   msfuy
-REAL , DIMENSION( ims:ime, jms:jme) ,         INTENT(IN   ) ::   msfvx
-REAL , DIMENSION( ims:ime, jms:jme) ,         INTENT(IN   ) ::   msfvy
-REAL , DIMENSION( ims:ime, jms:jme) ,         INTENT(IN   ) ::   msftx
-REAL , DIMENSION( ims:ime, jms:jme) ,         INTENT(IN   ) ::   msfty
-
-REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(INOUT) :: tendency
-
-REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(IN   ) ::         &
-                                   xkhh, &
-                                    rdz, &
-                                   rdzw, &
-                                    rho, &
-                                u_2, v_2   ! XS
-
-REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(IN   ) ::    var, &
-                                     zx, &
-                                     zy
-
-REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(IN   ) ::   rsfs_h1, rsfs_h2  ! XS
-
-
-REAL ,                                        INTENT(IN   ) ::    rdx, &
-                                    rdy
-
-! Local data
-
-INTEGER :: i, j, k, ktf
-
-INTEGER :: i_start, i_end, j_start, j_end
-
-REAL , DIMENSION( its-1:ite+1, kts:kte, jts-1:jte+1)    ::     H1avg, &
-                                 H2avg, &
-                                    H1, &
-                                    H2, &
-                                xkxavg, &
-                               zx_at_m, &
-                               zy_at_m
-
-REAL , DIMENSION( its:ite, kts:kte, jts:jte)            ::  tmptendf
-
-REAL    :: mrdx, mrdy, rcoup
-REAL    :: tmpzx, tmpzy, tmpzeta_z, rdzu, rdzv
-INTEGER :: ktes1,ktes2
-
-real :: ubar, vbar, dvar, rr, cr
-
-! End declarations.
-!-----------------------------------------------------------------------
-
-ktf=MIN(kte,kde-1)
-
-!-----------------------------------------------------------------------
-! scalars:   t (.), u(|), v(+), w(-)
-!
-!       t  u  t  u                               t  v  t  v
-!
-! w     -     3     -      k+1             w     -     3     -      k+1
-!
-! t     .  1  O  1  .      k               t     .  2  O  2  .      k
-!
-! w     -     3     -      k               w     -     3     -      k
-!
-! t     .  |  .  |  .      k-1             t     .  +  .  +  .      k-1
-!
-! w     -     -     -      k-1             w     -     -     -      k-1
-!
-! t    i-1 i  i i+1                             j-1 j  j j+1
-!
-
-ktes1=kte-1
-ktes2=kte-2
-
-i_start = its
-i_end   = MIN(ite,ide-1)
-j_start = jts
-j_end   = MIN(jte,jde-1)
-
-IF ( config_flags%open_xs .or. config_flags%specified .or. &
-config_flags%nested) i_start = MAX(ids+1,its)
-IF ( config_flags%open_xe .or. config_flags%specified .or. &
-config_flags%nested) i_end   = MIN(ide-2,ite)
-IF ( config_flags%open_ys .or. config_flags%specified .or. &
-config_flags%nested) j_start = MAX(jds+1,jts)
-IF ( config_flags%open_ye .or. config_flags%specified .or. &
-config_flags%nested) j_end   = MIN(jde-2,jte)
-IF ( config_flags%periodic_x ) i_start = its
-IF ( config_flags%periodic_x ) i_end = MIN(ite,ide-1)
-
-! diffusion of the TKE needs mutiple 2
-
-IF ( doing_tke ) THEN
-DO j = j_start, j_end
-DO k = kts,ktf
-DO i = i_start, i_end
-tmptendf(i,k,j)=tendency(i,k,j)
-ENDDO
-ENDDO
-ENDDO
-ENDIF
-
-! H1 = partial var over partial x
-
-!  if (config_flags%rsfs_opt.ge.0 .and. config_flags%rsfs_opt.lt.6 .and. config_flags%sfs_opt.gt.0) THEN
-
-!     H1 = 0.0
-
-!  ELSE
-DO j = j_start, j_end
-DO k = kts, ktf
-DO i = i_start, i_end + 1
-xkxavg(i,k,j)=0.5*(xkhh(i-1,k,j)+xkhh(i,k,j))*0.5*(rho(i-1,k,j)+rho(i,k,j))
-ENDDO
-ENDDO
-ENDDO
-
-DO j = j_start, j_end
-DO k = kts+1, ktf
-DO i = i_start, i_end + 1
-H1avg(i,k,j)=0.5*(fnm(k)*(var(i-1,k  ,j)+var(i,k  ,j))+  &
-fnp(k)*(var(i-1,k-1,j)+var(i,k-1,j)))
-ENDDO
-ENDDO
-ENDDO
-
-DO j = j_start, j_end
-DO i = i_start, i_end + 1
-H1avg(i,kts  ,j)=0.5*(cf1*var(i  ,1,j)+cf2*var(i  ,2,j)+ &
-cf3*var(i  ,3,j)+cf1*var(i-1,1,j)+  &
-cf2*var(i-1,2,j)+cf3*var(i-1,3,j))
-H1avg(i,ktf+1,j)=0.5*(var(i,ktes1,j)+(var(i,ktes1,j)- &
-var(i,ktes2,j))*0.5*dnw(ktes1)/dn(ktes1)+ &
-var(i-1,ktes1,j)+(var(i-1,ktes1,j)- &
-var(i-1,ktes2,j))*0.5*dnw(ktes1)/dn(ktes1))
-ENDDO
-ENDDO
-
-DO j = j_start, j_end
-DO k = kts, ktf
-DO i = i_start, i_end + 1
-! new
-tmpzx = 0.5*( zx(i,k,j)+ zx(i,k+1,j))
-rdzu = 2./(1./rdzw(i,k,j) + 1./rdzw(i-1,k,j))
-H1(i,k,j)=-msfux(i,j)*xkxavg(i,k,j)*(                      &
-rdx*(var(i,k,j)-var(i-1,k,j)) - tmpzx*         &
-(H1avg(i,k+1,j)-H1avg(i,k,j))*rdzu )
-
-!      tmpzeta_z = 0.5*(zeta_z(i,j)+zeta_z(i-1,j))
-!      H1(i,k,j)=-msfuy(i,j)*xkxavg(i,k,j)*(                         &
-!                 rdx*(var(i,k,j)-var(i-1,k,j)) - tmpzx*tmpzeta_z*  &
-!                     (H1avg(i,k+1,j)-H1avg(i,k,j))/dnw(k))
-ENDDO
-ENDDO
-ENDDO
-!  END IF
-
-IF ( config_flags%rsfs_opt .lt. 6 .and. config_flags%rsfs_opt .ge. 0 ) THEN ! ADD RSFS STRESSES
-
-DO j = j_start, j_end
-DO k = kts, ktf
-DO i = i_start, i_end + 1
-if ((k-kts).lt.config_flags%drm_dwl_klevel .or. config_flags%drm_dwl_klevel.eq.0) then ! ASW don't add rsfs above some k level for numerical stability
-
-!! ASW DWL the easy way
-!IF ( config_flags%rsfsT_opt .eq. 1) THEN
-    H1(i,k,j) = H1(i,k,j) + rsfs_h1(i,k,j)
-!ELSE IF ( config_flags%rsfsT_opt .eq. 0) THEN
-!H1(i,k,j) = H1(i,k,j)
-!END IF
-    ! check for now to make sure this is working correctly
-    !if ((i.eq.i_start+5).and.(j.eq.j_start+5).and.(k.eq.kts+5)) print *, 'k=', k, 'kts=', kts, 'adding rtau'
-!else
-    !if ((i.eq.i_start+5).and.(j.eq.j_start+5).and.(k.eq.kts+100)) print *, 'k=', k, 'kts=', kts, 'not adding rtau'
-
-end if
-ENDDO
-ENDDO
-ENDDO
-
-!ASW
-if (config_flags%dwlsurfsmooth_opt .eq. 1) then
-
-! YW and XS
-do j = j_start-1, j_end+1
-  do k = kts, ktf
-     do i = i_start-1, i_end+1
-        ubar = u_2(i, k, j)
-        dvar = var(i, k, j) - var(i-1, k, j)
-        if (abs(dvar) .lt. 1.0e-8) then
-           if (dvar .ge. 0.0) then
-              dvar = 1.0e-8
-           else
-              dvar = -1.0e-8
-           end if
-        end if
-
-        if (ubar .ge. 0) then
-           rr = (var(i-1,k,j) - var(i-2,k,j)) / dvar
-        else
-           rr = (var(i+1,k,j) - var(i,k,j)) / dvar
-        end if
-
-        cr = max(0.0, min(1.0, rr))
-
-        H1(i, k, j) = H1(i, k, j) * cr
-
-        if ((k-kts).lt.4) then
-          H1(i, k, j) = 0.0
-        else if ((k-kts)>3 .and. (k-kts)<7) then
-          H1(i, k, j) = H1(i, k, j) * (real(k)-3.0)/4.0
-        end if
-
-     end do
-  end do
-end do
-
-end if !ASW
-
-
-ENDIF
-
-
-
-! H2 = partial var over partial y
-
-!  if (config_flags%rsfs_opt.ge.0 .and. config_flags%rsfs_opt.lt.6 .and. config_flags%sfs_opt.gt.0) THEN
-!     H2 =0.0
-!  ELSE
-DO j = j_start, j_end + 1
-DO k = kts, ktf
-DO i = i_start, i_end
-xkxavg(i,k,j)=0.5*(xkhh(i,k,j-1)+xkhh(i,k,j))*0.5*(rho(i,k,j-1)+rho(i,k,j))
-ENDDO
-ENDDO
-ENDDO
-
-DO j = j_start, j_end + 1
-DO k = kts+1,   ktf
-DO i = i_start, i_end
-H2avg(i,k,j)=0.5*(fnm(k)*(var(i,k  ,j-1)+var(i,k  ,j))+  &
-fnp(k)*(var(i,k-1,j-1)+var(i,k-1,j)))
-ENDDO
-ENDDO
-ENDDO
-
-DO j = j_start, j_end + 1
-DO i = i_start, i_end
-H2avg(i,kts  ,j)=0.5*(cf1*var(i,1,j  )+cf2*var(i  ,2,j)+ &
-cf3*var(i,3,j  )+cf1*var(i,1,j-1)+  &
-cf2*var(i,2,j-1)+cf3*var(i,3,j-1))
-H2avg(i,ktf+1,j)=0.5*(var(i,ktes1,j)+(var(i,ktes1,j)- &
-var(i,ktes2,j))*0.5*dnw(ktes1)/dn(ktes1)+ &
-var(i,ktes1,j-1)+(var(i,ktes1,j-1)- &
-var(i,ktes2,j-1))*0.5*dnw(ktes1)/dn(ktes1))
-ENDDO
-ENDDO
-
-DO j = j_start, j_end + 1
-DO k = kts, ktf
-DO i = i_start, i_end
-! new
-tmpzy = 0.5*( zy(i,k,j)+ zy(i,k+1,j))
-rdzv = 2./(1./rdzw(i,k,j) + 1./rdzw(i,k,j-1))
-H2(i,k,j)=-msfvy(i,j)*xkxavg(i,k,j)*(                       &
-rdy*(var(i,k,j)-var(i,k,j-1)) - tmpzy*          &
-(H2avg(i ,k+1,j)-H2avg(i,k,j))*rdzv)
-
-!      tmpzeta_z = 0.5*(zeta_z(i,j)+zeta_z(i,j-1))
-!      H2(i,k,j)=-msfvy(i,j)*xkxavg(i,k,j)*(                         &
-!                 rdy*(var(i,k,j)-var(i,k,j-1)) - tmpzy*tmpzeta_z*  &
-!                     (H2avg(i ,k+1,j)-H2avg(i,k,j))/dnw(k))
-ENDDO
-ENDDO
-ENDDO
-!  END IF
-
-IF ( config_flags%rsfs_opt .lt. 6 .and. config_flags%rsfs_opt .ge. 0 ) THEN ! ADD RSFS STRESSES
-
-DO j = j_start, j_end + 1
-DO k = kts, ktf
-DO i = i_start, i_end
-if ((k-kts).lt.config_flags%drm_dwl_klevel .or. config_flags%drm_dwl_klevel.eq.0) then ! ASW don't add rsfs above some k level for numerical stability
-
-!! ASW DWL the easy way
-!IF ( config_flags%rsfsT_opt .eq. 1) THEN
-H2(i,k,j) = H2(i,k,j) + rsfs_h2(i,k,j)
-!ELSE IF ( config_flags%rsfsT_opt .eq. 0) THEN
-!H2(i,k,j) = H2(i,k,j)
-!END IF
-
-end if
-ENDDO
-ENDDO
-ENDDO
-
-!ASW
-if (config_flags%dwlsurfsmooth_opt .eq. 1) then
-
-! YW and XS
-do j = j_start-1, j_end+1
-  do k = kts, ktf
-     do i = i_start-1, i_end+1
-        vbar = v_2(i, k, j)
-        dvar = var(i, k, j) - var(i, k, j-1)
-        if (abs(dvar) .lt. 1.0e-8) then
-           if (dvar .ge. 0.0) then
-              dvar = 1.0e-8
-           else
-              dvar = -1.0e-8
-           end if
-        end if
-
-        if (vbar .ge. 0) then
-           rr = (var(i,k,j-1) - var(i,k,j-2)) / dvar
-        else
-           rr = (var(i,k,j+1) - var(i,k,j)) / dvar
-        end if
-
-        cr = max(0.0, min(1.0, rr))
-
-        H2(i, k, j) = H2(i, k, j) * cr
-
-        if ((k-kts).lt.4) then
-          H2(i, k, j) = 0.0
-        else if ((k-kts)>3 .and. (k-kts)<7) then
-          H2(i, k, j) = H2(i, k, j) * (real(k)-3.0)/4.0
-        end if
-
-     end do
-  end do
-end do
-
-end if !ASW
-
-ENDIF
-
-
-DO j = j_start, j_end
-DO k = kts+1, ktf
-DO i = i_start, i_end
-H1avg(i,k,j)=0.5*(fnm(k)*(H1(i+1,k  ,j)+H1(i,k  ,j))+  &
-fnp(k)*(H1(i+1,k-1,j)+H1(i,k-1,j)))
-H2avg(i,k,j)=0.5*(fnm(k)*(H2(i,k  ,j+1)+H2(i,k  ,j))+  &
-fnp(k)*(H2(i,k-1,j+1)+H2(i,k-1,j)))
-! new
-!     zxavg(i,k,j)=fnm(k)*zx(i,k,j)+fnp(k)*zx(i,k-1,j)
-!     zyavg(i,k,j)=fnm(k)*zy(i,k,j)+fnp(k)*zy(i,k-1,j)
-
-! H1avg(i,k,j)=zx*H1avg*zeta_z
-! H2avg(i,k,j)=zy*H2avg*zeta_z
-
-zx_at_m(i, k, j) = 0.25*(zx(i,k,j)+ zx(i+1,k,j) + zx(i,k+1,j)+ zx(i+1,k+1,j))
-zy_at_m(i, k, j) = 0.25*(zy(i,k,j)+ zy(i,k,j+1) + zy(i,k+1,j)+ zy(i,k+1,j+1))
-
-!      H1avg(i,k,j)=H1avg(i,k,j)*tmpzx*zeta_z(i,j)
-!      H2avg(i,k,j)=H2avg(i,k,j)*tmpzy*zeta_z(i,j)
-ENDDO
-ENDDO
-ENDDO
-
-DO j = j_start, j_end
-DO i = i_start, i_end
-H1avg(i,kts  ,j)=0.
-H1avg(i,ktf+1,j)=0.
-H2avg(i,kts  ,j)=0.
-H2avg(i,ktf+1,j)=0.
-zx_at_m(i, kts, j) = 0.25*(zx(i,kts,j)+ zx(i+1,kts,j) + zx(i,kts+1,j)+ zx(i+1,kts+1,j))
-zy_at_m(i, kts, j) = 0.25*(zy(i,kts,j)+ zy(i,kts,j+1) + zy(i,kts+1,j)+ zy(i,kts+1,j+1))
-
-ENDDO
-ENDDO
-
-DO j = j_start, j_end
-DO k = kts,ktf
-DO i = i_start, i_end
-
-mrdx=msftx(i,j)*rdx
-mrdy=msfty(i,j)*rdy
-
-tendency(i,k,j)=tendency(i,k,j) +   g/(dnw(k)*rdzw(i,k,j)) * &
-(mrdx*(H1(i+1,k,j)-H1(i  ,k,j)) +                       &
-mrdy*(H2(i,k,j+1)-H2(i,k,j  )) -                       &
-msftx(i,j)*zx_at_m(i, k, j)*(H1avg(i,k+1,j)-H1avg(i,k,j))*rdzw(i,k,j) - &
-msfty(i,j)*zy_at_m(i, k, j)*(H2avg(i,k+1,j)-H2avg(i,k,j))*rdzw(i,k,j)   &
-)
-ENDDO
-ENDDO
-ENDDO
-
-IF ( doing_tke ) THEN
-DO j = j_start, j_end
-DO k = kts,ktf
-DO i = i_start, i_end
-tendency(i,k,j)=tmptendf(i,k,j)+2.* &
-(tendency(i,k,j)-tmptendf(i,k,j))
-ENDDO
-ENDDO
-ENDDO
-ENDIF
-
-END SUBROUTINE rsfs_horizontal_diffusion_s
-
-
 !=======================================================================
 !=======================================================================
 
 SUBROUTINE vertical_diffusion_2   ( ru_tendf, rv_tendf, rw_tendf, rt_tendf,   &
                                     tke_tendf, moist_tendf, n_moist,          &
                                     chem_tendf, n_chem,                       &
                                     scalar_tendf, n_scalar,                   &
                                     tracer_tendf, n_tracer,                   &
                                     u_2, v_2,                                 &
                                     thp,u_base,v_base,t_base,qv_base,tke,     &
                                     theta,                                    &
                                     config_flags,defor13,defor23,defor33,     &
                                     nba_mij, n_nba_mij,                       &
-                                    rsfs_rmij, n_rsfs_rmij,                   &   ! XS  
-                                    rsfs_qmij, n_rsfs_qmij,                   &   ! XS  
-                                    rsfs_qsij, n_rsfs_qsij,                   &   ! XS  
-                                    nwtau13, nwtau23,                         & !JDM    
-									div,                                      &
+                                    div,                                      &
                                     moist,chem,scalar,tracer,                 &
                                     xkmv,xkhv,xkmh,km_opt,                    & ! xkmh added
                                     fnm, fnp, dn, dnw, rdz, rdzw,             &
                                     hfx, qfx, ust, rho,                       &
                                     ids, ide, jds, jde, kds, kde,             &
                                     ims, ime, jms, jme, kms, kme,             &
                                     its, ite, jts, jte, kts, kte              )
 
 !-----------------------------------------------------------------------
 ! Begin declarations.
 
    IMPLICIT NONE
 
    TYPE(grid_config_rec_type), INTENT(IN   ) :: config_flags
 
    INTEGER ,        INTENT(IN   ) ::        ids, ide, jds, jde, kds, kde, &
                                             ims, ime, jms, jme, kms, kme, &
                                             its, ite, jts, jte, kts, kte
 
    INTEGER ,        INTENT(IN   ) ::        n_moist,n_chem,n_scalar,n_tracer,km_opt
 
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) :: fnm
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) :: fnp
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) :: dnw
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::  dn
 
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) :: qv_base
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::  u_base
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::  v_base
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::  t_base
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(INOUT) ::ru_tendf,&
                                                                  rv_tendf,&
                                                                  rw_tendf,&
                                                                 tke_tendf,&
                                                                 rt_tendf
 
-   REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(IN) ::    nwtau13,& 
-                                                                  nwtau23   
-
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme, n_moist),                 &
           INTENT(INOUT) ::                                    moist_tendf
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme, n_chem),                  &
           INTENT(INOUT) ::                                     chem_tendf
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme, n_scalar) ,               &
           INTENT(INOUT) ::                                   scalar_tendf
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme, n_tracer) ,               &
           INTENT(INOUT) ::                                   tracer_tendf
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme, n_moist),                 &
           INTENT(INOUT) ::                                          moist
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme, n_chem),                  &
           INTENT(INOUT) ::                                           chem
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme, n_scalar) ,               &
           INTENT(IN   ) ::                                         scalar
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme, n_tracer) ,               &
           INTENT(IN   ) ::                                         tracer
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(IN   ) ::defor13, &
                                                                  defor23, &
                                                                  defor33, &
                                                                      div, &
                                                                     xkmv, &
                                                                     xkhv, &
                                                                     xkmh, &
                                                                      tke, &
                                                                    theta, &
                                                                      rdz, &
                                                                      u_2, &
                                                                      v_2, &
                                                                     rdzw
 
-   INTEGER, INTENT(  IN ) :: n_nba_mij, &
-	   n_rsfs_rmij, n_rsfs_qmij, n_rsfs_qsij   ! XS
+   INTEGER, INTENT(  IN ) :: n_nba_mij
 
    REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_nba_mij), INTENT(INOUT) &
    :: nba_mij
-   REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_rsfs_rmij), INTENT(INOUT) &   ! XS    
-   :: rsfs_rmij                                                                        
-   REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_rsfs_qmij), INTENT(INOUT) &   ! XS    
-   :: rsfs_qmij                                                                        
-   REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_rsfs_qsij), INTENT(INOUT) &   ! XS    
-   :: rsfs_qsij                                                                        
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(IN   )   :: rho
    REAL , DIMENSION( ims:ime, jms:jme), INTENT(INOUT)            :: hfx,  &
                                                                     qfx
    REAL , DIMENSION( ims:ime, jms:jme), INTENT(IN   )            :: ust
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(INOUT) ::    thp
 
 ! LOCAL VAR
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme)  ::    var_mix
 
    INTEGER :: im, i,j,k
    INTEGER :: i_start, i_end, j_start, j_end
 
 !  REAL , DIMENSION( its:ite, kts:kte, jts:jte) :: xkhv
 
 !***************************************************************************
 !***************************************************************************
 !MODIFICA VARIABILI PER I FLUSSI
 !
@@ -4960,62 +4114,57 @@ SUBROUTINE vertical_diffusion_2   ( ru_tendf, rv_tendf, rw_tendf, rt_tendf,   &
     REAL :: heat_flux, moist_flux, heat_flux0
     REAL :: cpm
 !
 !FINE MODIFICA VARIABILI PER I FLUSSI
 !***************************************************************************
 !
 
 ! End declarations.
 !-----------------------------------------------------------------------
 
    i_start = its
    i_end   = MIN(ite,ide-1)
    j_start = jts
    j_end   = MIN(jte,jde-1)
 !
 !-----------------------------------------------------------------------
 
       CALL vertical_diffusion_u_2( ru_tendf, config_flags,        &
                                    defor13, xkmv,                 &
                                    nba_mij, n_nba_mij,            &
-								   rsfs_rmij, n_rsfs_rmij,        &   ! XS
-								   nwtau13,                       & !JDM
                                    dnw, rdzw, fnm, fnp, rho,      &
                                    ids, ide, jds, jde, kds, kde,  &
                                    ims, ime, jms, jme, kms, kme,  &
                                    its, ite, jts, jte, kts, kte  )
 
 
       CALL vertical_diffusion_v_2( rv_tendf, config_flags,        &
                                    defor23, xkmv,                 &
                                    nba_mij, n_nba_mij,            &
-								   rsfs_rmij, n_rsfs_rmij,        &   ! XS   
-                                   nwtau23,                       & !JDM 
-								   dnw, rdzw, fnm, fnp, rho,      &
+                                   dnw, rdzw, fnm, fnp, rho,      &
                                    ids, ide, jds, jde, kds, kde,  &
                                    ims, ime, jms, jme, kms, kme,  &
                                    its, ite, jts, jte, kts, kte  )
 
       CALL vertical_diffusion_w_2( rw_tendf, config_flags,        &
                                    defor33, tke(ims,kms,jms),     &
                                    nba_mij, n_nba_mij,            &
-								   rsfs_rmij, n_rsfs_rmij,        &   ! XS
                                    div, xkmh,                     &
                                    dn, rdz, fnm, fnp, rho,        &
                                    ids, ide, jds, jde, kds, kde,  &
                                    ims, ime, jms, jme, kms, kme,  &
                                    its, ite, jts, jte, kts, kte  )
 
 !*****************************************
 !*****************************************
 !  MODIFICA al flusso di momento alla parete
 !
   vflux: SELECT CASE( config_flags%isfflx )
   CASE (0) ! Assume cd a constant, specified in namelist
     cd0 = config_flags%tke_drag_coefficient  ! constant drag coefficient
                                              ! set in namelist.input
 !
 !calcolo del modulo della velocita
     DO j = j_start, j_end
     DO i = i_start, ite
        V0_u=0.
        tao_xz=0.
@@ -5034,41 +4183,41 @@ SUBROUTINE vertical_diffusion_2   ( ru_tendf, rv_tendf, rw_tendf, rt_tendf,   &
     ENDDO
 !
     DO j = j_start, jte
     DO i = i_start, i_end
        V0_v=0.
        tao_yz=0.
        V0_v=    sqrt((v_2(i,kts,j)**2) +         &
                         (((u_2(i  ,kts,j  )+          &
                            u_2(i  ,kts,j-1)+          &
                            u_2(i+1,kts,j  )+          &
                            u_2(i+1,kts,j-1))/4)**2))+epsilon
 
        tao_yz=cd0*V0_v*v_2(i,kts,j)
        rv_tendf(i,kts,j)=rv_tendf(i,kts,j) +   g*tao_yz*0.5*(rho(i,kts,j)+rho(i,kts,j-1))/dnw(kts)
        IF ( (config_flags%m_opt .EQ. 1) .OR. (config_flags%sfs_opt .GT. 0) ) THEN
           nba_mij(i,kts,j,P_m23) = -tao_yz
        ENDIF
     ENDDO
     ENDDO
 
-  CASE (1,2,3) ! ustar computed from surface routine
+  CASE (1,2) ! ustar computed from surface routine
     DO j = j_start, j_end
     DO i = i_start, ite
        V0_u=0.
        tao_xz=0.
        V0_u=    sqrt((u_2(i,kts,j)**2) +         &
                         (((v_2(i  ,kts,j  )+          &
                            v_2(i  ,kts,j+1)+          &
                            v_2(i-1,kts,j  )+          &
                            v_2(i-1,kts,j+1))/4)**2))+epsilon
        ustar=0.5*(ust(i,j)+ust(i-1,j))
 
        tao_xz=ustar*ustar*u_2(i,kts,j)/V0_u
        ru_tendf(i,kts,j)=ru_tendf(i,kts,j) +   g*tao_xz*0.5*(rho(i,kts,j)+rho(i-1,kts,j))/dnw(kts)
        IF ( (config_flags%m_opt .EQ. 1) .OR. (config_flags%sfs_opt .GT. 0) ) THEN
           nba_mij(i,kts,j,P_m13) = -tao_xz
        ENDIF
     ENDDO
     ENDDO
 
     DO j = j_start, jte
@@ -5103,162 +4252,88 @@ SUBROUTINE vertical_diffusion_2   ( ru_tendf, rv_tendf, rw_tendf, rt_tendf,   &
 
      DO j=jts,min(jte,jde-1)
      DO k=kts,kte-1
      DO i=its,min(ite,ide-1)
        var_mix(i,k,j) = thp(i,k,j)
      ENDDO
      ENDDO
      ENDDO
 
    ELSE
 
      DO j=jts,min(jte,jde-1)
      DO k=kts,kte-1
      DO i=its,min(ite,ide-1)
        var_mix(i,k,j) = thp(i,k,j) - t_base(k)
      ENDDO
      ENDDO
      ENDDO
 
    END IF
- 
-   if ( config_flags%rsfs_opt .ge. 6 .or. config_flags%rsfs_opt .lt. 0) THEN ! no RSFS ASW  
-      CALL vertical_diffusion_s( rt_tendf, config_flags, var_mix, xkhv, &         
-                                  dn, dnw, rdz, rdzw, fnm, fnp, rho,     &        
-                                  .false.,                               &        
-                                  ids, ide, jds, jde, kds, kde,          &        
-                                  ims, ime, jms, jme, kms, kme,          &        
-                                  its, ite, jts, jte, kts, kte          )         
-   else    ! add RSFS                                                             
-      CALL rsfs_vertical_diffusion_s( rt_tendf, rsfs_rmij(ims,kms,jms,P_rh3), &  !
-                                  config_flags, var_mix, xkhv, &                  
-                                  dn, dnw, rdz, rdzw, fnm, fnp, rho,     &        
-                                  .false.,                               &        
-                                  ids, ide, jds, jde, kds, kde,          &        
-                                  ims, ime, jms, jme, kms, kme,          &        
-                                  its, ite, jts, jte, kts, kte          )         
-   end if                                                                         
-
-   IF ( ( config_flags%sfs_opt .GT. 0 ) .OR. ( config_flags%m_opt .EQ. 1 ) ) THEN !ASW
-
-!JDM-MMC-------------------------------------------------------------------------
-!JDM-MMC
-!JDM-MMC  Subroutine *_diffusion_s (where * is either "vertical" or "horizontal")
-!JDM-MMC  is a generic subroutine that is called to calculate the tendency due to
-!JDM-MMC  turbulent diffusion for any scalar --heat, moisture, tke, any chemical
-!JDM-MMC  species or any explicit scalar. The tendency is computed via divergence
-!JDM-MMC  of the flux, with the fluxes computed within the subroutine.
-!JDM-MMC
-!JDM-MMC  In order to output the flux of the quantity in question, a variable
-!JDM-MMC  to retreive that flux must also be passed into the subroutine
-!JDM-MMC  However, we might want fluxes of only certain quantities, e.g. heat
-!JDM-MMC  and moisture, but not all scalars, chem species, etc. What to do?
-!JDM-MMC   
-!JDM-MMC  The following 4 options come to mind:
-!JDM-MMC   
-!JDM-MMC  --To continue to use the same subroutines for all scalars--, either
-!JDM-MMC
-!JDM-MMC  1) Pass in dummy variables as a placeholders for all the other scalars
-!JDM-MMC     for which we do not want fluxes, so the argument list is identical
-!JDM-MMC     for all calls.
-!JDM-MMC  2) Use optional variables.
-!JDM-MMC  3) A two-step process:
-!JDM-MMC     i) Call the exising subroutines to compute the tendencies as usual 
-!JDM-MMC     ii) Duplicate and rename the existing subroutines and modify those
-!JDM-MMC     to only return the desired fluxes, but not recompute the tendencies.
-!JDM-MMC     Call the new subroutines after the calls to the originals.
-!JDM-MMC
-!JDM-MMC  --Alternatively--,
-!JDM-MMC    
-!JDM-MMC  4) Duplicate, rename and modify the subroutines so that those can be
-!JDM-MMC     used to compute both the tendencies and output the fluxes for
-!JDM-MMC     the subset of scalars for which one wants the fluxes.
-!JDM-MMC     Use the existing unmodified subroutines for all the others.
-!JDM-MMC
-!JDM-MMC  Since I do not know how to use optional variables, and to keep the 
-!JDM-MMC  coding as disentangled as possible, I went with option 3. There is
-!JDM-MMC  some waste involved in computing the flux a second time just for
-!JDM-MMC  output, however the computations are pretty lightweight *no MPI). 
-!JDM-MMC
-!JDM-MMC  I made a copies of subroutines *_diffusion_s called 
-!JDM-MMC  *_diffusion_s_get_flux, which identically compute and also return 
-!JDM-MMC  the flux. So far this is only enabled for sensible heat flux, but
-!JDM-MMC  can be extended easily to other species.
-!JDM-MMC   
-!JDM-MMC-------------------------------------------------------------------------
-   
-      CALL vertical_diffusion_s_get_flux( nba_mij(ims,kms,jms,P_mh3),            &
-                                          config_flags, var_mix, xkhv,           &
-                                          dn, dnw, rdz, rdzw, fnm, fnp, rho,     &
-                                          .false.,                               &
-                                          ids, ide, jds, jde, kds, kde,          &
-                                          ims, ime, jms, jme, kms, kme,          &
-                                          its, ite, jts, jte, kts, kte          )
- 
-   ENDIF
+
+   CALL vertical_diffusion_s( rt_tendf, config_flags, var_mix, xkhv, &
+                              dn, dnw, rdz, rdzw, fnm, fnp, rho,     &
+                              .false.,                               &
+                              ids, ide, jds, jde, kds, kde,          &
+                              ims, ime, jms, jme, kms, kme,          &
+                              its, ite, jts, jte, kts, kte          )
 
 
-   
 !*****************************************
 !*****************************************
 !MODIFICA al flusso di calore
 !
 !
   hflux: SELECT CASE( config_flags%isfflx )
   CASE (0,2) ! with fixed surface heat flux given in the namelist
     heat_flux = config_flags%tke_heat_flux  ! constant heat flux value
                                             ! set in namelist.input
     DO j = j_start, j_end
     DO i = i_start, i_end
        cpm = cp * (1. + 0.8 * moist(i,kts,j,P_QV))
        hfx(i,j)=heat_flux*cpm*rho(i,kts,j)         ! provided for output only
        if(config_flags%use_theta_m == 1)THEN
          rt_tendf(i,kts,j)=rt_tendf(i,kts,j)  &
              -g*heat_flux*(1.+rvovrd*moist(i,kts,j,P_QV))*rho(i,kts,j)/dnw(kts) &
              -g*1.61*theta(i,kts,j)*qfx(i,j)/dnw(kts)
        ELSE
          rt_tendf(i,kts,j)=rt_tendf(i,kts,j)  &
              -g*heat_flux*rho(i,kts,j)/dnw(kts)
        ENDIF
-       IF ( (config_flags%m_opt .EQ. 1) .OR. (config_flags%sfs_opt .GT. 0) ) THEN !JDM-MMC ASW
-          nba_mij(i,kts,j,P_mh3) = heat_flux
-       ENDIF
     ENDDO
     ENDDO
 
-  CASE (1,3) ! use surface heat flux computed from surface routine
+  CASE (1) ! use surface heat flux computed from surface routine
     DO j = j_start, j_end
     DO i = i_start, i_end
 
        cpm = cp * (1. + 0.8 * moist(i,kts,j,P_QV))
        heat_flux = hfx(i,j)/cpm
        if(config_flags%use_theta_m == 1)THEN
          rt_tendf(i,kts,j)=rt_tendf(i,kts,j)  &
              -g*heat_flux*(1.+rvovrd*moist(i,kts,j,P_QV))/dnw(kts) &
              -g*1.61*theta(i,kts,j)*qfx(i,j)/dnw(kts)
        ELSE
          rt_tendf(i,kts,j)=rt_tendf(i,kts,j)  &
              -g*heat_flux/dnw(kts)
        ENDIF
-       IF ( (config_flags%m_opt .EQ. 1) .OR. (config_flags%sfs_opt .GT. 0) ) THEN !JDM-MMC ASW
-          nba_mij(i,kts,j,P_mh3) = heat_flux
-       ENDIF
+
     ENDDO
     ENDDO
 
   CASE DEFAULT
     CALL wrf_error_fatal( 'isfflx value invalid for diff_opt=2' )
   END SELECT hflux
 
 !
 ! FINE MODIFICA al flusso di calore
 !*****************************************
 !*****************************************
 
    If (km_opt .eq. 2) then
    CALL vertical_diffusion_s( tke_tendf(ims,kms,jms),               &
                               config_flags, tke(ims,kms,jms),       &
                               xkhv,                                 &
                               dn, dnw, rdz, rdzw, fnm, fnp, rho,    &
                               .true.,                               &
                               ids, ide, jds, jde, kds, kde,         &
                               ims, ime, jms, jme, kms, kme,         &
@@ -5274,73 +4349,59 @@ SUBROUTINE vertical_diffusion_2   ( ru_tendf, rv_tendf, rw_tendf, rt_tendf,   &
          DO j=jts,min(jte,jde-1)
          DO k=kts,kte-1
          DO i=its,min(ite,ide-1)
           var_mix(i,k,j) = moist(i,k,j,im) - qv_base(k)
          ENDDO
          ENDDO
          ENDDO
 
        ELSE
 
          DO j=jts,min(jte,jde-1)
          DO k=kts,kte-1
          DO i=its,min(ite,ide-1)
           var_mix(i,k,j) = moist(i,k,j,im)
          ENDDO
          ENDDO
          ENDDO
 
        END IF
 
-	   if ( config_flags%rsfs_opt .ge. 6 .or. config_flags%rsfs_opt .lt. 0) THEN ! no RSFS ASW  
-          CALL vertical_diffusion_s(moist_tendf(ims,kms,jms,im),         &                      
-                                    config_flags, var_mix,               &                      
-                                    xkhv,                                &                      
-                                    dn, dnw, rdz, rdzw, fnm, fnp, rho,   &                      
-                                    .false.,                             &                      
-                                    ids, ide, jds, jde, kds, kde,        &                      
-                                    ims, ime, jms, jme, kms, kme,        &                      
-                                    its, ite, jts, jte, kts, kte        )                       
-       else                                                                                     
-          CALL rsfs_vertical_diffusion_s( moist_tendf(ims,kms,jms,im),    &                     
-                                    rsfs_qmij(ims,kms,jms,(im-PARAM_FIRST_SCALAR)*3+3),     &   
-                                    config_flags, var_mix,               &                      
-                                    xkhv,                                &                      
-                                    dn, dnw, rdz, rdzw, fnm, fnp, rho,   &                      
-                                    .false.,                             &                      
-                                    ids, ide, jds, jde, kds, kde,        &                      
-                                    ims, ime, jms, jme, kms, kme,        &                      
-                                    its, ite, jts, jte, kts, kte        )                       
-       end if                                                                                   
-
-
 
+          CALL vertical_diffusion_s( moist_tendf(ims,kms,jms,im),         &
+                                     config_flags, var_mix,               &
+                                     xkhv,                                &
+                                     dn, dnw, rdz, rdzw, fnm, fnp, rho,   &
+                                     .false.,                             &
+                                     ids, ide, jds, jde, kds, kde,        &
+                                     ims, ime, jms, jme, kms, kme,        &
+                                     its, ite, jts, jte, kts, kte        )
 
 !*****************************************
 !*****************************************
 !MODIFICATIONS for water vapor flux
 !
 !
   qflux: SELECT CASE( config_flags%isfflx )
   CASE (0)
 ! do nothing
-  CASE (1,2,3) ! with surface moisture flux
+  CASE (1,2) ! with surface moisture flux
     IF ( im == P_QV ) THEN
        DO j = j_start, j_end
        DO i = i_start, i_end
           moist_flux = qfx(i,j)
           moist_tendf(i,kts,j,im)=moist_tendf(i,kts,j,im)  &
                -g*moist_flux/dnw(kts)
        ENDDO
        ENDDO
     ENDIF
 
   CASE DEFAULT
     CALL wrf_error_fatal( 'isfflx value invalid for diff_opt=2' )
   END SELECT qflux
 !
 ! END MODIFICATIONS for water vapor flux
 !*****************************************
 !*****************************************
      ENDDO moist_loop
 
    ENDIF
@@ -5365,112 +4426,90 @@ SUBROUTINE vertical_diffusion_2   ( ru_tendf, rv_tendf, rw_tendf, rt_tendf,   &
 
      tracer_loop: do im = PARAM_FIRST_SCALAR, n_tracer
 
           CALL vertical_diffusion_s( tracer_tendf(ims,kms,jms,im),         &
                                      config_flags, tracer(ims,kms,jms,im), &
                                      xkhv,                                 &
                                      dn, dnw, rdz, rdzw, fnm, fnp, rho,    &
                                      .false.,                              &
                                      ids, ide, jds, jde, kds, kde,         &
                                      ims, ime, jms, jme, kms, kme,         &
                                      its, ite, jts, jte, kts, kte         )
      ENDDO tracer_loop
 
    ENDIF
 
 
    IF (n_scalar .ge. PARAM_FIRST_SCALAR) THEN
 
      scalar_loop: do im = PARAM_FIRST_SCALAR, n_scalar
 
-     if (config_flags%rsfs_opt.lt.0 .or. config_flags%rsfs_opt.ge.6) then   ! no RSFS ASW         
-          CALL vertical_diffusion_s(scalar_tendf(ims,kms,jms,im),         &                    
-                                    config_flags, scalar(ims,kms,jms,im), &                    
-                                    xkhv,                                 &                    
-                                    dn, dnw, rdz, rdzw, fnm, fnp, rho,    &                    
-                                    .false.,                              &                    
-                                    ids, ide, jds, jde, kds, kde,         &                    
-                                    ims, ime, jms, jme, kms, kme,         &                    
-                                    its, ite, jts, jte, kts, kte         )                     
-     else                                                                                      
-          CALL rsfs_vertical_diffusion_s( scalar_tendf(ims,kms,jms,im),         &              
-                                    rsfs_qsij(ims,kms,jms,(im-PARAM_FIRST_SCALAR)*3+3),   &    
-                                    config_flags, scalar(ims,kms,jms,im), &                    
-                                    xkhv,                                 &                    
-                                    dn, dnw, rdz, rdzw, fnm, fnp, rho,    &                    
-                                    .false.,                              &                    
-                                    ids, ide, jds, jde, kds, kde,         &                    
-                                    ims, ime, jms, jme, kms, kme,         &                    
-                                    its, ite, jts, jte, kts, kte         )                     
-     end if                                                                                    								 
-								 
-	 ENDDO scalar_loop
+          CALL vertical_diffusion_s( scalar_tendf(ims,kms,jms,im),         &
+                                     config_flags, scalar(ims,kms,jms,im), &
+                                     xkhv,                                 &
+                                     dn, dnw, rdz, rdzw, fnm, fnp, rho,    &
+                                     .false.,                              &
+                                     ids, ide, jds, jde, kds, kde,         &
+                                     ims, ime, jms, jme, kms, kme,         &
+                                     its, ite, jts, jte, kts, kte         )
+     ENDDO scalar_loop
 
    ENDIF
 
 END SUBROUTINE vertical_diffusion_2
 
 !=======================================================================
 !=======================================================================
 
 SUBROUTINE vertical_diffusion_u_2( tendency, config_flags,                &
                                    defor13, xkmv,                         &
                                    nba_mij, n_nba_mij,                    &
-								   rsfs_rmij, n_rsfs_rmij,                &   ! XS
-								   nwtau13,                               & !JDM
                                    dnw, rdzw, fnm, fnp, rho,              &
                                    ids, ide, jds, jde, kds, kde,          &
                                    ims, ime, jms, jme, kms, kme,          &
                                    its, ite, jts, jte, kts, kte          )
 
 !-----------------------------------------------------------------------
 ! Begin declarations.
 
    IMPLICIT NONE
 
    TYPE(grid_config_rec_type), INTENT(IN   ) :: config_flags
 
    INTEGER ,         INTENT(IN   ) ::       ids, ide, jds, jde, kds, kde, &
                                             ims, ime, jms, jme, kms, kme, &
                                             its, ite, jts, jte, kts, kte
 
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnm
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnp
    REAL , DIMENSION( kms:kme ) ,            INTENT(IN   )      :: dnw
 !   REAL , DIMENSION( ims:ime , jms:jme ) ,  INTENT(IN   )      :: zeta_z
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(INOUT) ::tendency
 
    REAL , DIMENSION( ims:ime , kms:kme, jms:jme ) ,                       &
                                             INTENT(IN   )      ::defor13, &
                                                                     xkmv, &
                                                                     rdzw, &
                                                                      rho
 
-   INTEGER, INTENT(  IN ) :: n_nba_mij, &
-							 n_rsfs_rmij   ! XS
-
-   REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_rsfs_rmij), INTENT(INOUT) &   ! XS      
-   :: rsfs_rmij                                                                          
-                                                                                         
-   REAL , DIMENSION(ims:ime, kms:kme, jms:jme), INTENT( IN ) &   !JDM                    
-   :: nwtau13                                                                            
+   INTEGER, INTENT(  IN ) :: n_nba_mij
 
    REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_nba_mij), INTENT(INOUT) &
    :: nba_mij
 
 ! LOCAL VARS
 
    INTEGER :: i, j, k, ktf
 
    INTEGER :: i_start, i_end, j_start, j_end
    INTEGER :: is_ext,ie_ext,js_ext,je_ext
 
    REAL , DIMENSION( its-1:ite+1, kts:kte, jts-1:jte+1)        :: titau3
 
    REAL , DIMENSION( its:ite, jts:jte)                         ::  zzavg
 
    REAL :: rdzu
 
 ! End declarations.
 !-----------------------------------------------------------------------
 
@@ -5482,120 +4521,110 @@ SUBROUTINE vertical_diffusion_u_2( tendency, config_flags,                &
    j_end   = MIN(jte,jde-1)
 
    IF ( config_flags%open_xs .or. config_flags%specified .or. &
         config_flags%nested) i_start = MAX(ids+1,its)
    IF ( config_flags%open_xe .or. config_flags%specified .or. &
         config_flags%nested) i_end   = MIN(ide-1,ite)
    IF ( config_flags%open_ys .or. config_flags%specified .or. &
         config_flags%nested) j_start = MAX(jds+1,jts)
    IF ( config_flags%open_ye .or. config_flags%specified .or. &
         config_flags%nested) j_end   = MIN(jde-2,jte)
       IF ( config_flags%periodic_x ) i_start = its
       IF ( config_flags%periodic_x ) i_end = ite
 
 ! titau3 = titau13
    is_ext=0
    ie_ext=0
    js_ext=0
    je_ext=0
    CALL cal_titau_13_31( config_flags, titau3, defor13,   &
                          nba_mij(ims,kms,jms,P_m13),      &
-						 rsfs_rmij(ims,kms,jms,P_rm13),   &   ! XS
-						 nwtau13,                         & !JDM
                          xkmv, fnm, fnp, rho,             &
                          is_ext, ie_ext, js_ext, je_ext,  &
                          ids, ide, jds, jde, kds, kde,    &
                          ims, ime, jms, jme, kms, kme,    &
                          its, ite, jts, jte, kts, kte     )
 !
       DO j = j_start, j_end
       DO k=kts+1,ktf
       DO i = i_start, i_end
 
          rdzu = -g/(dnw(k))
          tendency(i,k,j)=tendency(i,k,j)-rdzu*(titau3(i,k+1,j)-titau3(i,k,j))
 
       ENDDO
       ENDDO
       ENDDO
 
 ! ******** MODIF...
 !  we will pick up the surface drag (titau3(i,kts,j)) later
 !
        DO j = j_start, j_end
        k=kts
        DO i = i_start, i_end
 
           rdzu = -g/dnw(k)
           tendency(i,k,j)=tendency(i,k,j)-rdzu*(titau3(i,k+1,j))
        ENDDO
        ENDDO
 ! ******** MODIF...
 
 END SUBROUTINE vertical_diffusion_u_2
 
 !=======================================================================
 !=======================================================================
 
 SUBROUTINE vertical_diffusion_v_2( tendency, config_flags,                &
                                    defor23, xkmv,                         &
                                    nba_mij, n_nba_mij,                    &
-								   rsfs_rmij, n_rsfs_rmij,                &   ! XS
-								   nwtau23,                               & !JDM
                                    dnw, rdzw, fnm, fnp, rho,              &
                                    ids, ide, jds, jde, kds, kde,          &
                                    ims, ime, jms, jme, kms, kme,          &
                                    its, ite, jts, jte, kts, kte          )
 !-----------------------------------------------------------------------
 ! Begin declarations.
 
    IMPLICIT NONE
 
    TYPE(grid_config_rec_type), INTENT(IN   ) :: config_flags
 
    INTEGER ,         INTENT(IN   ) ::       ids, ide, jds, jde, kds, kde, &
                                             ims, ime, jms, jme, kms, kme, &
                                             its, ite, jts, jte, kts, kte
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnm
    REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnp
    REAL , DIMENSION( kms:kme ) ,            INTENT(IN   )      :: dnw
 !   REAL , DIMENSION( ims:ime , jms:jme ) ,  INTENT(IN   )      :: zeta_z
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(INOUT) ::tendency
 
    REAL , DIMENSION( ims:ime , kms:kme, jms:jme ) ,                       &
                                             INTENT(IN   )      ::defor23, &
                                                                     xkmv, &
                                                                     rdzw, &
                                                                      rho
 
-   INTEGER, INTENT(  IN ) :: n_nba_mij, n_rsfs_rmij   ! XS 
-
-   REAL , DIMENSION(ims:ime, kms:kme, jms:jme), INTENT( IN ) &   !JDM    
-   :: nwtau23                                                            
+   INTEGER, INTENT(  IN ) :: n_nba_mij
 
    REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_nba_mij),  INTENT(INOUT) &
    :: nba_mij
 
-   REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_rsfs_rmij),  INTENT(INOUT) &   !XS  
-   :: rsfs_rmij                                                                      
-                                                                                     
 ! LOCAL VARS
 
    INTEGER :: i, j, k, ktf
 
    INTEGER :: i_start, i_end, j_start, j_end
    INTEGER :: is_ext,ie_ext,js_ext,je_ext
 
    REAL , DIMENSION( its-1:ite+1, kts:kte, jts-1:jte+1)        :: titau3
 
    REAL , DIMENSION( its:ite, jts:jte)                         ::  zzavg
 
    REAL  :: rdzv
 
 ! End declarations.
 !-----------------------------------------------------------------------
 
    ktf=MIN(kte,kde-1)
 
    i_start = its
    i_end   = MIN(ite,ide-1)
@@ -5603,159 +4632,151 @@ SUBROUTINE vertical_diffusion_v_2( tendency, config_flags,                &
    j_end   = jte
 
    IF ( config_flags%open_xs .or. config_flags%specified .or. &
         config_flags%nested) i_start = MAX(ids+1,its)
    IF ( config_flags%open_xe .or. config_flags%specified .or. &
         config_flags%nested) i_end   = MIN(ide-2,ite)
    IF ( config_flags%open_ys .or. config_flags%specified .or. &
         config_flags%nested) j_start = MAX(jds+1,jts)
    IF ( config_flags%open_ye .or. config_flags%specified .or. &
         config_flags%nested) j_end   = MIN(jde-1,jte)
       IF ( config_flags%periodic_x ) i_start = its
       IF ( config_flags%periodic_x ) i_end = MIN(ite,ide-1)
 
 ! titau3 = titau23
    is_ext=0
    ie_ext=0
    js_ext=0
    je_ext=0
    CALL cal_titau_23_32( config_flags, titau3, defor23,   &
                          nba_mij(ims,kms,jms,P_m23),      &
-						 rsfs_rmij(ims,kms,jms,P_rm23),   &  ! XS
-						 nwtau23,                         & !JDM
                          xkmv, fnm, fnp, rho,             &
                          is_ext, ie_ext, js_ext, je_ext,  &
                          ids, ide, jds, jde, kds, kde,    &
                          ims, ime, jms, jme, kms, kme,    &
                          its, ite, jts, jte, kts, kte     )
 
    DO j = j_start, j_end
    DO k = kts+1,ktf
    DO i = i_start, i_end
 
       rdzv = - g / dnw(k)
       tendency(i,k,j)=tendency(i,k,j)-rdzv*(titau3(i,k+1,j)-titau3(i,k,j))
 
    ENDDO
    ENDDO
    ENDDO
 
 ! ******** MODIF...
 !  we will pick up the surface drag (titau3(i,kts,j)) later
 !
        DO j = j_start, j_end
        k=kts
        DO i = i_start, i_end
 
         rdzv = - g / dnw(k)
         tendency(i,k,j)=tendency(i,k,j)-rdzv*(titau3(i,k+1,j))
 
        ENDDO
        ENDDO
 ! ******** MODIF...
 
 END SUBROUTINE vertical_diffusion_v_2
 
 !=======================================================================
 !=======================================================================
 
 SUBROUTINE vertical_diffusion_w_2(tendency, config_flags,                 &
                                 defor33, tke,                             &
                                 nba_mij, n_nba_mij,                       &
-								rsfs_rmij, n_rsfs_rmij,                   &   ! XS
                                 div, xkmh,                                &
                                 dn, rdz, fnm, fnp, rho,                   &
                                 ids, ide, jds, jde, kds, kde,             &
                                 ims, ime, jms, jme, kms, kme,             &
                                 its, ite, jts, jte, kts, kte              )
 
 !-----------------------------------------------------------------------
 ! Begin declarations.
 
    IMPLICIT NONE
 
    TYPE(grid_config_rec_type), INTENT(IN   ) :: config_flags
 
    INTEGER ,         INTENT(IN   ) ::       ids, ide, jds, jde, kds, kde, &
                                             ims, ime, jms, jme, kms, kme, &
                                             its, ite, jts, jte, kts, kte
 
    REAL , DIMENSION( kms:kme ) ,            INTENT(IN   )      ::  dn, fnm, fnp
 
    REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(INOUT) ::tendency
 
    REAL , DIMENSION( ims:ime , kms:kme, jms:jme ) ,                       &
                                             INTENT(IN   )      ::defor33, &
                                                                      tke, &
                                                                      div, &
                                                                     xkmh, &
                                                                      rdz, &
                                                                      rho
 
-   INTEGER, INTENT(  IN ) :: n_nba_mij, n_rsfs_rmij   ! XS
+   INTEGER, INTENT(  IN ) :: n_nba_mij
 
    REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_nba_mij),  INTENT(INOUT) &
    :: nba_mij
 
-   REAL , DIMENSION(ims:ime, kms:kme, jms:jme, n_rsfs_rmij),  INTENT(IN) &   
-   :: rsfs_rmij  ! XS                                                        
-                                                                             
-
 ! LOCAL VARS
 
    INTEGER :: i, j, k, ktf
 
    INTEGER :: i_start, i_end, j_start, j_end
    INTEGER :: is_ext,ie_ext,js_ext,je_ext
 
    REAL , DIMENSION( its-1:ite+1, kts:kte, jts-1:jte+1)        :: titau3
 
 ! End declarations.
 !-----------------------------------------------------------------------
 
    ktf=MIN(kte,kde-1)
 
    i_start = its
    i_end   = MIN(ite,ide-1)
    j_start = jts
    j_end   = MIN(jte,jde-1)
 
    IF ( config_flags%open_xs .or. config_flags%specified .or. &
         config_flags%nested) i_start = MAX(ids+1,its)
    IF ( config_flags%open_xe .or. config_flags%specified .or. &
         config_flags%nested) i_end   = MIN(ide-2,ite)
    IF ( config_flags%open_ys .or. config_flags%specified .or. &
         config_flags%nested) j_start = MAX(jds+1,jts)
    IF ( config_flags%open_ye .or. config_flags%specified .or. &
         config_flags%nested) j_end   = MIN(jde-2,jte)
       IF ( config_flags%periodic_x ) i_start = its
       IF ( config_flags%periodic_x ) i_end = MIN(ite,ide-1)
 
 ! titau3 = titau33
    is_ext=0
    ie_ext=0
    js_ext=0
    je_ext=0
    CALL cal_titau_11_22_33( config_flags, titau3,            &
                             tke, xkmh, defor33,              &
                             nba_mij(ims,kms,jms,P_m33), rho, &
-							rsfs_rmij(ims,kms,jms,P_rm33),   &   ! XS
                             is_ext, ie_ext, js_ext, je_ext,  &
                             ids, ide, jds, jde, kds, kde,    &
                             ims, ime, jms, jme, kms, kme,    &
                             its, ite, jts, jte, kts, kte     )
 
 !   DO j = j_start, j_end
 !   DO k = kts+1, ktf
 !   DO i = i_start, i_end
 !      titau3(i,k,j)=titau3(i,k,j)*zeta_z(i,j)
 !   ENDDO
 !   ENDDO
 !   ENDDO
 
    DO j = j_start, j_end
    DO k = kts+1, ktf
    DO i = i_start, i_end
        tendency(i,k,j)=tendency(i,k,j)+   g*(titau3(i,k,j)-titau3(i,k-1,j))/dn(k)
    ENDDO
    ENDDO
    ENDDO
@@ -5870,364 +4891,119 @@ SUBROUTINE vertical_diffusion_s( tendency, config_flags, var, xkhv,       &
                        +   g * (H3(i,k+1,j)-H3(i,k,j))/dnw(k)
    ENDDO
    ENDDO
    ENDDO
 
    IF (doing_tke) THEN
       DO j = j_start, j_end
       DO k = kts,ktf
       DO i = i_start, i_end
           tendency(i,k,j)=tmptendf(i,k,j)+2.* &
                           (tendency(i,k,j)-tmptendf(i,k,j))
       ENDDO
       ENDDO
       ENDDO
    ENDIF
 
 END SUBROUTINE vertical_diffusion_s
 
 !=======================================================================
 !=======================================================================
-! XS
-
-SUBROUTINE rsfs_vertical_diffusion_s( tendency, rtau, config_flags, var, xkhv,       &
-                                 dn, dnw, rdz, rdzw, fnm, fnp, rho,       &
-                                 doing_tke,                               &
-                                 ids, ide, jds, jde, kds, kde,            &
-                                 ims, ime, jms, jme, kms, kme,            &
-                                 its, ite, jts, jte, kts, kte            )
-
+ SUBROUTINE nonlocal_flux (config_flags,nlflux,gamu,gamv,hpbl,kpbl,     &
+                                  dx,dy,dt,ust,hfx,qfx,br,ep1,ep2,      &
+                                  karman,u_phy,v_phy,th_phy,rho,moist,n_moist, &
+                                  msftx,msfty,rdzw,                            &
+                                  u10,v10,wspd,                                &
+                                  ids, ide, jds, jde, kds, kde,     &
+                                  ims, ime, jms, jme, kms, kme,     &
+                                  its, ite, jts, jte, kts, kte      )
+!-----------------------------------------------------------------------
+!This subroutine prescibes the nonlocal heat flux profile based on LES analysis
+!And compute the nonlocal momentum gamma term 
+!References:
+! Shin and Hong 2015, MWR
+! Xu Zhang et al. 2018, MWR  
 !-----------------------------------------------------------------------
 ! Begin declarations.
-
+ 
    IMPLICIT NONE
-
+ 
    TYPE(grid_config_rec_type), INTENT(IN   ) :: config_flags
-
+ 
    INTEGER ,         INTENT(IN   ) ::       ids, ide, jds, jde, kds, kde, &
                                             ims, ime, jms, jme, kms, kme, &
                                             its, ite, jts, jte, kts, kte
 
-   LOGICAL,         INTENT(IN   ) ::        doing_tke
+   INTEGER ,         INTENT(IN   ) :: n_moist
 
-   REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnm
-   REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnp
-   REAL , DIMENSION( kms:kme ) ,            INTENT(IN   )      ::  dn
-   REAL , DIMENSION( kms:kme ) ,            INTENT(IN   )      :: dnw
+   REAL,    INTENT(IN )     ::      ep1,ep2,karman
 
-   REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(INOUT) ::tendency
+   REAL,    INTENT(IN )     ::      dx,dy,dt
 
-   REAL , DIMENSION( ims:ime , kms:kme, jms:jme ) , INTENT(IN) ::   xkhv
+   REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT(IN) ::   &
+   th_phy,u_phy,v_phy
 
-   REAL , DIMENSION( ims:ime , kms:kme, jms:jme ) ,                       &
-                                            INTENT(IN   )      ::    var, &
-                                                                     rdz, &
-                                                                    rdzw, &
-                                                                    rho
+   REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT(IN) :: rdzw
 
-    REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT( IN )  &
-    :: rtau
+   REAL, DIMENSION( ims:ime, kms:kme, jms:jme, n_moist ), INTENT( INOUT ) :: &
+   moist
 
-! LOCAL VARS
+   REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT(IN) :: rho
+ 
+   REAL, DIMENSION( ims:ime, jms:jme ), INTENT(INOUT)   :: hfx,qfx,br,ust,hpbl
 
-   INTEGER :: i, j, k, ktf
+   REAL, DIMENSION( ims:ime, jms:jme ), INTENT(IN   )   :: u10,v10,wspd
 
-   INTEGER :: i_start, i_end, j_start, j_end
+   REAL, DIMENSION( ims:ime, jms:jme ), INTENT(IN   )   :: msftx,msfty
 
-   REAL , DIMENSION( its:ite, kts:kte, jts:jte)            ::        H3, &
-                                                                 xkxavg, &
-                                                                  rravg
+   REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT(OUT) :: nlflux
 
-   REAL , DIMENSION( its:ite, kts:kte, jts:jte)            ::  tmptendf
+   REAL, DIMENSION( ims:ime, jms:jme ), INTENT(OUT   )  :: gamu,gamv
 
-! End declarations.
-!-----------------------------------------------------------------------
+   INTEGER, DIMENSION( ims:ime, jms:jme ), INTENT(INOUT   )  :: kpbl
 
-   ktf=MIN(kte,kde-1)
-
-   i_start = its
-   i_end   = MIN(ite,ide-1)
-   j_start = jts
-   j_end   = MIN(jte,jde-1)
-
-   IF ( config_flags%open_xs .or. config_flags%specified .or. &
-        config_flags%nested) i_start = MAX(ids+1,its)
-   IF ( config_flags%open_xe .or. config_flags%specified .or. &
-        config_flags%nested) i_end   = MIN(ide-2,ite)
-   IF ( config_flags%open_ys .or. config_flags%specified .or. &
-        config_flags%nested) j_start = MAX(jds+1,jts)
-   IF ( config_flags%open_ye .or. config_flags%specified .or. &
-        config_flags%nested) j_end   = MIN(jde-2,jte)
-      IF ( config_flags%periodic_x ) i_start = its
-      IF ( config_flags%periodic_x ) i_end = MIN(ite,ide-1)
-
-   IF (doing_tke) THEN
-      DO j = j_start, j_end
-      DO k = kts,ktf
-      DO i = i_start, i_end
-         tmptendf(i,k,j)=tendency(i,k,j)
-      ENDDO
-      ENDDO
-      ENDDO
-   ENDIF
-
-! H3
-
-  !  if (config_flags%rsfs_opt.ge.0 .and. config_flags%rsfs_opt.lt.6 .and. config_flags%sfs_opt.gt.0) THEN
-
-  !     H3 = 0.0
-  !  ELSE
-   xkxavg = 0.
-
-   DO j = j_start, j_end
-   DO k = kts+1,ktf
-   DO i = i_start, i_end
-      xkxavg(i,k,j)=fnm(k)*xkhv(i,k,j)+fnp(k)*xkhv(i,k-1,j)
-      xkxavg(i,k,j)=xkxavg(i,k,j)*(fnm(k)*rho(i,k,j)+fnp(k)*rho(i,k-1,j))
-      H3(i,k,j)=-xkxavg(i,k,j)*(var(i,k,j)-var(i,k-1,j))*rdz(i,k,j)
-   ENDDO
-   ENDDO
-   ENDDO
-
-   DO j = j_start, j_end
-   DO i = i_start, i_end
-      H3(i,kts,j)=0.
-      H3(i,ktf+1,j)=0.
-   ENDDO
-   ENDDO
-  !  END IF
-
-   DO j = j_start, j_end
-      DO k = kts+1,ktf
-         DO i = i_start, i_end
-            if ((k-kts).lt.config_flags%drm_dwl_klevel .or. config_flags%drm_dwl_klevel.eq.0) then ! ASW don't add rsfs above some k level for numerical stability
-
-			!! ASW DWL the easy way
-			!IF ( config_flags%rsfsT_opt .eq. 1) THEN
-            !print*,'adding rsfs h3!' !ASW
-			H3(i,k,j) = H3(i,k,j) + rtau(i,k,j)
-			!ELSE IF ( config_flags%rsfsT_opt .eq. 0) THEN
-			!H3(i,k,j) = H3(i,k,j)
-			!END IF
-
-            end if
-         END DO
-      END DO
-   END DO
-
-
-
-   DO j = j_start, j_end
-   DO k = kts,ktf
-   DO i = i_start, i_end
-      tendency(i,k,j)=tendency(i,k,j)  &
-                       +   g * (H3(i,k+1,j)-H3(i,k,j))/dnw(k)
-   ENDDO
-   ENDDO
-   ENDDO
-
-   IF (doing_tke) THEN
-      DO j = j_start, j_end
-      DO k = kts,ktf
-      DO i = i_start, i_end
-          tendency(i,k,j)=tmptendf(i,k,j)+2.* &
-                          (tendency(i,k,j)-tmptendf(i,k,j))
-      ENDDO
-      ENDDO
-      ENDDO
-   ENDIF
-
-END SUBROUTINE rsfs_vertical_diffusion_s
-
-!=======================================================================
-
-!ASW
-
-SUBROUTINE vertical_diffusion_s_get_flux( flux3,                               &
-                                          config_flags, var, xkhv,             &
-                                          dn, dnw, rdz, rdzw, fnm, fnp, rho,   &
-                                          doing_tke,                           &
-                                          ids, ide, jds, jde, kds, kde,        &
-                                          ims, ime, jms, jme, kms, kme,        &
-                                          its, ite, jts, jte, kts, kte         )
-  
-!JDM-MMC Subroutine to return fluxes for output 
-!-----------------------------------------------------------------------
-! Begin declarations.
-
-   IMPLICIT NONE
-
-   TYPE(grid_config_rec_type), INTENT(IN   ) :: config_flags
-
-   INTEGER ,         INTENT(IN   ) ::       ids, ide, jds, jde, kds, kde, &
-                                            ims, ime, jms, jme, kms, kme, &
-                                            its, ite, jts, jte, kts, kte
-
-   LOGICAL,         INTENT(IN   ) ::        doing_tke
-
-   REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnm
-   REAL , DIMENSION( kms:kme ) ,                 INTENT(IN   ) ::    fnp
-   REAL , DIMENSION( kms:kme ) ,            INTENT(IN   )      ::  dn
-   REAL , DIMENSION( kms:kme ) ,            INTENT(IN   )      :: dnw
-
-   REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(OUT  ) :: flux3
-
-   REAL , DIMENSION( ims:ime , kms:kme, jms:jme ) , INTENT(IN) ::   xkhv
-
-   REAL , DIMENSION( ims:ime , kms:kme, jms:jme ) ,                       &
-                                            INTENT(IN   )      ::    var, &
-                                                                     rdz, &
-                                                                    rdzw, &
-                                                                     rho
-! LOCAL VARS
-
-   INTEGER :: i, j, k, ktf
-
-   INTEGER :: i_start, i_end, j_start, j_end
-
-   REAL , DIMENSION( its:ite, kts:kte, jts:jte)            ::    xkxavg, &
-                                                                  rravg
-
-! End declarations.
-!-----------------------------------------------------------------------
-
-   ktf=MIN(kte,kde-1)
-
-   i_start = its
-   i_end   = MIN(ite,ide-1)
-   j_start = jts
-   j_end   = MIN(jte,jde-1)
-
-   IF ( config_flags%open_xs .or. config_flags%specified .or. &
-        config_flags%nested) i_start = MAX(ids+1,its)
-   IF ( config_flags%open_xe .or. config_flags%specified .or. &
-        config_flags%nested) i_end   = MIN(ide-2,ite)
-   IF ( config_flags%open_ys .or. config_flags%specified .or. &
-        config_flags%nested) j_start = MAX(jds+1,jts)
-   IF ( config_flags%open_ye .or. config_flags%specified .or. &
-        config_flags%nested) j_end   = MIN(jde-2,jte)
-      IF ( config_flags%periodic_x ) i_start = its
-      IF ( config_flags%periodic_x ) i_end = MIN(ite,ide-1)
-
-! H3
-
-   xkxavg = 0.
-
-   DO j = j_start, j_end
-   DO k = kts+1,ktf
-   DO i = i_start, i_end
-      xkxavg(i,k,j)=fnm(k)*xkhv(i,k,j)+fnp(k)*xkhv(i,k-1,j)
-      xkxavg(i,k,j)=xkxavg(i,k,j)*(fnm(k)*rho(i,k,j)+fnp(k)*rho(i,k-1,j))
-      flux3(i,k,j)=-xkxavg(i,k,j)*(var(i,k,j)-var(i,k-1,j))*rdz(i,k,j)
-   ENDDO
-   ENDDO
-   ENDDO
-
-   DO j = j_start, j_end
-   DO i = i_start, i_end
-      flux3(i,kts,j)=0.
-      flux3(i,ktf+1,j)=0.
-   ENDDO
-   ENDDO
-
-END SUBROUTINE vertical_diffusion_s_get_flux
-
-!=======================================================================
- SUBROUTINE nonlocal_flux (config_flags,nlflux,gamu,gamv,hpbl,kpbl,     &
-                                  dx,dy,dt,ust,hfx,qfx,br,ep1,ep2,      &
-                                  karman,u_phy,v_phy,th_phy,rho,moist,n_moist, &
-                                  msftx,msfty,rdzw,                            &
-                                  u10,v10,wspd,                                &
-                                  ids, ide, jds, jde, kds, kde,     &
-                                  ims, ime, jms, jme, kms, kme,     &
-                                  its, ite, jts, jte, kts, kte      )
-!-----------------------------------------------------------------------
-!This subroutine prescibes the nonlocal heat flux profile based on LES analysis
-!And compute the nonlocal momentum gamma term 
-!References:
-! Shin and Hong 2015, MWR
-! Xu Zhang et al. 2018, MWR  
-!-----------------------------------------------------------------------
-! Begin declarations.
- 
-   IMPLICIT NONE
- 
-   TYPE(grid_config_rec_type), INTENT(IN   ) :: config_flags
- 
-   INTEGER ,         INTENT(IN   ) ::       ids, ide, jds, jde, kds, kde, &
-                                            ims, ime, jms, jme, kms, kme, &
-                                            its, ite, jts, jte, kts, kte
-
-   INTEGER ,         INTENT(IN   ) :: n_moist
-
-   REAL,    INTENT(IN )     ::      ep1,ep2,karman
-
-   REAL,    INTENT(IN )     ::      dx,dy,dt
-
-   REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT(IN) ::   &
-   th_phy,u_phy,v_phy
-
-   REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT(IN) :: rdzw
-
-   REAL, DIMENSION( ims:ime, kms:kme, jms:jme, n_moist ), INTENT( INOUT ) :: &
-   moist
-
-   REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT(IN) :: rho
- 
-   REAL, DIMENSION( ims:ime, jms:jme ), INTENT(INOUT)   :: hfx,qfx,br,ust,hpbl
-
-   REAL, DIMENSION( ims:ime, jms:jme ), INTENT(IN   )   :: u10,v10,wspd
-
-   REAL, DIMENSION( ims:ime, jms:jme ), INTENT(IN   )   :: msftx,msfty
-
-   REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT(OUT) :: nlflux
-
-   REAL, DIMENSION( ims:ime, jms:jme ), INTENT(OUT   )  :: gamu,gamv
-
-   INTEGER, DIMENSION( ims:ime, jms:jme ), INTENT(INOUT   )  :: kpbl
-
-! Local variables.
-  REAL, DIMENSION( its:ite, kts:kte,   jts:jte ) :: zq
-  REAL, DIMENSION( its:ite, kts:kte-1, jts:jte ) :: za,thv
-  REAL, DIMENSION( its:ite, kts:kte,   jts:jte ) :: zfacmf,entfacmf
-  REAL, DIMENSION( its:ite, jts:jte )            :: govrth,sflux,wstar3,wstar,rigs
-  LOGICAL, DIMENSION( its:ite,jts:jte )          :: pblflg, sfcflg, stable
-  REAL, DIMENSION( its:ite, jts:jte )            :: deltaoh,we,enlfrac2
-  REAL, DIMENSION( its:ite, jts:jte )            :: hfxpbl,bfxpbl
-  REAL, DIMENSION( its:ite, jts:jte )            :: dthv,wm2
-  REAL, DIMENSION( its:ite, jts:jte )            :: wscale,thermal
-  REAL               :: tvcon,delb,cpm,wm3,bfx0,ust3
-  REAL               :: dtheta,du,dv,thsfc,brint
-  INTEGER            :: i,j,k,i_start,i_end,j_start,j_end,ktf
-  REAL               :: mlfrac,ezfrac,sfcfracn,sflux0,snlflux0
-  REAL               :: amf1,amf2,amf3,bmf2,bmf3,pth1,delxy,pu1
-  REAL               :: heat_flux
-  REAL,PARAMETER     :: phifac = 8.,sfcfrac = 0.1,d1 = 0.02, d2 = 0.05, zfmin = 1.e-8
-  REAL,PARAMETER     :: h1 = 0.33333335, h2 = 0.6666667, tmin = 1.e-2
-! tunable parameters
-  REAL,PARAMETER     ::  mltop = 1.0, sfcfracn1 = 0.075
-  REAL,PARAMETER     ::  nlfrac = 0.68 ! the ratio of nonlocal heat flux to total heat flux
-  REAL,PARAMETER     ::  enlfrac = -0.15 
-  REAL,PARAMETER     ::  a11 = 1.0, a12 = -1.15
-  REAL,PARAMETER     ::  ezfac = 1.5
-  REAL,PARAMETER     ::  cpent = -0.4, rigsmax = 100., rimin = -100.
-  REAL,PARAMETER     ::  entfmin = 1.0, entfmax = 5.0, sm = 10.9
+! Local variables.
+  REAL, DIMENSION( its:ite, kts:kte,   jts:jte ) :: zq
+  REAL, DIMENSION( its:ite, kts:kte-1, jts:jte ) :: za,thv
+  REAL, DIMENSION( its:ite, kts:kte,   jts:jte ) :: zfacmf,entfacmf
+  REAL, DIMENSION( its:ite, jts:jte )            :: govrth,sflux,wstar3,wstar,rigs
+  LOGICAL, DIMENSION( its:ite,jts:jte )          :: pblflg, sfcflg, stable
+  REAL, DIMENSION( its:ite, jts:jte )            :: deltaoh,we,enlfrac2
+  REAL, DIMENSION( its:ite, jts:jte )            :: hfxpbl,bfxpbl
+  REAL, DIMENSION( its:ite, jts:jte )            :: dthv,wm2
+  REAL, DIMENSION( its:ite, jts:jte )            :: wscale,thermal
+  REAL               :: tvcon,delb,cpm,wm3,bfx0,ust3
+  REAL               :: dtheta,du,dv,thsfc,brint
+  INTEGER            :: i,j,k,i_start,i_end,j_start,j_end,ktf
+  REAL               :: mlfrac,ezfrac,sfcfracn,sflux0,snlflux0
+  REAL               :: amf1,amf2,amf3,bmf2,bmf3,pth1,delxy,pu1
+  REAL               :: heat_flux
+  REAL,PARAMETER     :: phifac = 8.,sfcfrac = 0.1,d1 = 0.02, d2 = 0.05, zfmin = 1.e-8
+  REAL,PARAMETER     :: h1 = 0.33333335, h2 = 0.6666667, tmin = 1.e-2
+! tunable parameters
+  REAL,PARAMETER     ::  mltop = 1.0, sfcfracn1 = 0.075
+  REAL,PARAMETER     ::  nlfrac = 0.68 ! the ratio of nonlocal heat flux to total heat flux
+  REAL,PARAMETER     ::  enlfrac = -0.15 
+  REAL,PARAMETER     ::  a11 = 1.0, a12 = -1.15
+  REAL,PARAMETER     ::  ezfac = 1.5
+  REAL,PARAMETER     ::  cpent = -0.4, rigsmax = 100., rimin = -100.
+  REAL,PARAMETER     ::  entfmin = 1.0, entfmax = 5.0, sm = 10.9
 
    ktf=MIN(kte,kde-1)
  
    i_start = its
    i_end   = MIN(ite,ide-1)
    j_start = jts
    j_end   = MIN(jte,jde-1)
  
    IF ( config_flags%open_xs .or. config_flags%specified .or. &
         config_flags%nested) i_start = MAX(ids+1,its)
    IF ( config_flags%open_xe .or. config_flags%specified .or. &
         config_flags%nested) i_end   = MIN(ide-2,ite)
    IF ( config_flags%open_ys .or. config_flags%specified .or. &
         config_flags%nested) j_start = MAX(jds+1,jts)
    IF ( config_flags%open_ye .or. config_flags%specified .or. &
         config_flags%nested) j_end   = MIN(jde-2,jte)
    IF ( config_flags%periodic_x ) i_start = its
    IF ( config_flags%periodic_x ) i_end = MIN(ite,ide-1)
  
    DO j = j_start, j_end
@@ -6284,41 +5060,41 @@ END SUBROUTINE vertical_diffusion_s_get_flux
    ENDDO
    ENDDO
    ENDDO
 
    DO j = j_start,j_end
    DO i = i_start,i_end
      govrth(i,j) = g/th_phy(i,1,j)
    ENDDO
    ENDDO
 
   hflux: SELECT CASE( config_flags%isfflx )
   CASE (0,2) ! with fixed surface heat flux given in the namelist
     heat_flux = config_flags%tke_heat_flux  ! constant heat flux value
     DO j = j_start, j_end
     DO i = i_start, i_end
        cpm = cp * (1. + 0.8 * moist(i,kts,j,P_QV))
        hfx(i,j)= heat_flux*cpm*rho(i,1,j)
     ENDDO
     ENDDO
  
-  CASE (1,3) ! use surface heat flux computed from surface routine
+  CASE (1) ! use surface heat flux computed from surface routine
     DO j = j_start, j_end
     DO i = i_start, i_end
        cpm = cp * (1. + 0.8 * moist(i,kts,j,P_QV))
        heat_flux = hfx(i,j)/cpm/rho(i,1,j)
     ENDDO
     ENDDO
  
   CASE DEFAULT
     CALL wrf_error_fatal( 'isfflx value invalid for diff_opt=2' )
   END SELECT hflux
 
   DO j = j_start, j_end
   DO i = i_start, i_end
      kpbl(i,j)   = 1
      hpbl(i,j)   = zq(i,1,j)
      pblflg(i,j) = .true.
      sfcflg(i,j) = .true.
  
      cpm = cp * (1. + 0.8*moist(i,1,j,P_QV))
      sflux(i,j) = (hfx(i,j)/cpm)/rho(i,1,j)
@@ -6537,41 +5313,41 @@ END SUBROUTINE nonlocal_flux
       den = a3*(doh)**b1 + a4*(doh)**b2+a5
       pu  = num/den
    else
       pu = 1.
    endif
    pu  = max(pu,pmin)
    pu  = min(pu,pmax)
 
    IF(d.LE.100.) THEN  ! assume dx<=100 as LES
       pu = 0.0
    ENDIF
 
    RETURN
    END FUNCTION
 
 !=======================================================================
 !=======================================================================
 
     SUBROUTINE cal_titau_11_22_33( config_flags, titau,              &
                                    tke, xkx, defor,                  &
-                                   mtau, rho, rtau,                  &   ! XS added rtau 
+                                   mtau, rho,                        &
                                    is_ext, ie_ext, js_ext, je_ext,   &
                                    ids, ide, jds, jde, kds, kde,     &
                                    ims, ime, jms, jme, kms, kme,     &
                                    its, ite, jts, jte, kts, kte      )
 
 ! History:     Sep 2003  Changes by George Bryan and Jason Knievel, NCAR
 !              Oct 2001  Converted to mass core by Bill Skamarock, NCAR
 !              Aug 2000  Original code by Shu-Hua Chen, UC-Davis
 
 ! Purpose:     This routine calculates stress terms (taus) for use in
 !              the calculation of production of TKE by sheared wind
 
 ! References:  Klemp and Wilhelmson (JAS 1978)
 !              Deardorff (B-L Meteor 1980)
 !              Chen and Dudhia (NCAR WRF physics report 2000)
 
 ! Key:
 
 !-----------------------------------------------------------------------
 ! Begin declarations.
@@ -6581,44 +5357,40 @@ END SUBROUTINE nonlocal_flux
     TYPE( grid_config_rec_type ), INTENT( IN )  &
     :: config_flags
 
     INTEGER, INTENT( IN )  &
     :: ids, ide, jds, jde, kds, kde,  &
        ims, ime, jms, jme, kms, kme,  &
        its, ite, jts, jte, kts, kte
 
     INTEGER, INTENT( IN )  &
     :: is_ext, ie_ext, js_ext, je_ext
 
     REAL, DIMENSION( its-1:ite+1, kts:kte, jts-1:jte+1 ), INTENT( INOUT )  &
     :: titau
 
     REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT( IN )  &
     :: defor, xkx, tke, rho
 
     REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT( INOUT )  &
     :: mtau
 
-    REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT( IN )  &
-    :: rtau    ! XS
-
-
 ! Local variables.
 
     INTEGER  &
     :: i, j, k, ktf, i_start, i_end, j_start, j_end
 
 ! End declarations.
 !-----------------------------------------------------------------------
 
     ktf = MIN( kte, kde-1 )
 
     i_start = its
     i_end   = ite
     j_start = jts
     j_end   = jte
 
     IF ( config_flags%open_xs .OR. config_flags%specified .OR. &
          config_flags%nested) i_start = MAX( ids+1, its )
     IF ( config_flags%open_xe .OR. config_flags%specified .OR. &
          config_flags%nested) i_end   = MIN( ide-1, ite )
     IF ( config_flags%open_ys .OR. config_flags%specified .OR. &
@@ -6659,70 +5431,48 @@ END SUBROUTINE nonlocal_flux
         END DO
         END DO
         END DO
 
       ELSE !NO STRESS OUTPUT
 
         DO j = j_start, j_end
         DO k = kts, ktf
         DO i = i_start, i_end
 
           titau(i,k,j) = - rho(i,k,j) * xkx(i,k,j) * defor(i,k,j)
 
         END DO
         END DO
         END DO
 
       ENDIF
 
     ENDIF
 
-    IF ( config_flags%rsfs_opt .lt. 6 .and. config_flags%rsfs_opt .ge. 0 ) THEN   ! add RSFS  
-                                                                                              
-      DO j = j_start, j_end                                                                   
-      DO k = kts, ktf                                                                         
-      DO i = i_start, i_end
-         if ((k-kts).lt.config_flags%drm_dwl_klevel .or. config_flags%drm_dwl_klevel.eq.0) then ! ASW don't add rsfs above some k level for numerical stability
-
-
-		 !! ASW DWL the easy way
-		 !IF ( config_flags%rsfsU_opt .eq. 1) THEN
-         titau(i,k,j) = titau(i,k,j) + rho(i,k,j)*rtau(i,k,j)
-		 !ELSE IF ( config_flags%rsfsU_opt .eq. 0) THEN
-		 !titau(i,k,j) = titau(i,k,j)
-	 	 !END IF
-
-         end if
-      END DO                                                                                  
-      END DO                                                                                  
-      END DO                                                                                  
-                                                                                              
-   ENDIF  ! RSFS                                                                              	
-
     END SUBROUTINE cal_titau_11_22_33
 
 !=======================================================================
 !=======================================================================
 
     SUBROUTINE cal_titau_12_21( config_flags, titau,             &
                                 xkx, defor,                      &
-                                mtau, rho, rtau,                 &   ! XS added rtau
+                                mtau, rho,                       &
                                 is_ext, ie_ext, js_ext, je_ext,  &
                                 ids, ide, jds, jde, kds, kde,    &
                                 ims, ime, jms, jme, kms, kme,    &
                                 its, ite, jts, jte, kts, kte     )
 
 ! History:     Sep 2003   Modifications by George Bryan and Jason Knievel, NCAR
 !              Oct 2001   Converted to mass core by Bill Skamarock, NCAR
 !              Aug 2000   Original code by Shu-Hua Chen, UC-Davis
 
 ! Pusrpose     This routine calculates the stress terms (taus) for use in
 !              the calculation of production of TKE by sheared wind
 
 ! References:  Klemp and Wilhelmson (JAS 1978)
 !              Deardorff (B-L Meteor 1980)
 !              Chen and Dudhia (NCAR WRF physics report 2000)
 
 ! Key:
 
 !-----------------------------------------------------------------------
 ! Begin declarations.
@@ -6732,54 +5482,48 @@ END SUBROUTINE nonlocal_flux
     TYPE( grid_config_rec_type), INTENT( IN )  &
     :: config_flags
 
     INTEGER, INTENT( IN )  &
     :: ids, ide, jds, jde, kds, kde,  &
        ims, ime, jms, jme, kms, kme,  &
        its, ite, jts, jte, kts, kte
 
     INTEGER, INTENT( IN )  &
     :: is_ext, ie_ext, js_ext, je_ext
 
     REAL, DIMENSION( its-1:ite+1, kts:kte, jts-1:jte+1 ), INTENT( INOUT )  &
     :: titau
 
     REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT( IN )  &
     :: defor, xkx, rho
 
     REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT( INOUT )  &
     :: mtau
 
-    REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT( IN )  & 
-    :: rtau   ! XS                                                
-                                                                  
-
 ! Local variables.
 
     INTEGER  &
     :: i, j, k, ktf, i_start, i_end, j_start, j_end
 
     REAL, DIMENSION( its-1:ite+1, kts:kte, jts-1:jte+1 )  &
     :: xkxavg, rhoavg
 
-!	REAL :: rhoavg   ! XS  
-
 ! End declarations.
 !-----------------------------------------------------------------------
 
     ktf = MIN( kte, kde-1 )
 
 ! Needs one more point in the x and y directions.
 
     i_start = its
     i_end   = ite
     j_start = jts
     j_end   = jte
 
     IF ( config_flags%open_xs .OR. config_flags%specified .OR. &
          config_flags%nested ) i_start = MAX( ids+1, its )
     IF ( config_flags%open_xe .OR. config_flags%specified .OR. &
          config_flags%nested ) i_end   = MIN( ide-1, ite )
     IF ( config_flags%open_ys .OR. config_flags%specified .OR. &
          config_flags%nested ) j_start = MAX( jds+1, jts )
     IF ( config_flags%open_ye .OR. config_flags%specified .OR. &
          config_flags%nested ) j_end   = MIN( jde-1, jte )
@@ -6830,73 +5574,47 @@ END SUBROUTINE nonlocal_flux
         END DO
         END DO
         END DO
 
       ELSE ! NO STRESS OUTPUT
 
         DO j = j_start, j_end
         DO k = kts, ktf
         DO i = i_start, i_end
 
           titau(i,k,j) = - xkxavg(i,k,j) * defor(i,k,j)
 
         END DO
         END DO
         END DO
 
       ENDIF
 
     ENDIF
 
-    IF ( config_flags%rsfs_opt .lt. 6 .and. config_flags%rsfs_opt .ge. 0 ) THEN ! add RSFS  
-                                                                                            
-      DO j = j_start, j_end                                                                 
-      DO k = kts, ktf                                                                       
-      DO i = i_start, i_end                                                                 
-!         rhoavg = 0.25 * ( rho(i-1,k,j  ) + rho(i,k,j  )  +  &                              
-!                           rho(i-1,k,j-1) + rho(i,k,j-1) )  
-         if ((k-kts).lt.config_flags%drm_dwl_klevel .or. config_flags%drm_dwl_klevel.eq.0) then ! ASW don't add rsfs above some k level for numerical stability
-
-
-         !! ASW DWL the easy way
-		 !IF ( config_flags%rsfsU_opt .eq. 1) THEN
-		 titau(i,k,j) = titau(i,k,j) + rhoavg(i,k,j)*rtau(i,k,j)
-		 !ELSE IF ( config_flags%rsfsU_opt .eq. 0) THEN
-		 !titau(i,k,j) = titau(i,k,j)
-		 !END IF
-
-         end if
-      END DO                                                                                
-      END DO                                                                                
-      END DO                                                                                
-                                                                                            
-    ENDIF ! RSFS                                                                            
-                                                                                            
-
     END SUBROUTINE cal_titau_12_21
 
 !=======================================================================
 
     SUBROUTINE cal_titau_13_31( config_flags, titau,             &
                                 defor,                           &
-                                mtau, rtau ,                     &   ! XS added rtau
-								ctau13,                          & !JDM
+                                mtau,                            &
                                 xkx, fnm, fnp, rho,              &
                                 is_ext, ie_ext, js_ext, je_ext,  &
                                 ids, ide, jds, jde, kds, kde,    &
                                 ims, ime, jms, jme, kms, kme,    &
                                 its, ite, jts, jte, kts, kte     )
 
 ! History:     Sep 2003   Modifications by George Bryan and Jason Knievel, NCAR
 !              Oct 2001   Converted to mass core by Bill Skamarock, NCAR
 !              Aug 2000   Original code by Shu-Hua Chen, UC-Davis
 
 ! Purpose:     This routine calculates the stress terms (taus) for use in
 !              the calculation of production of TKE by sheared wind
 
 ! References:  Klemp and Wilhelmson (JAS 1978)
 !              Deardorff (B-L Meteor 1980)
 !              Chen and Dudhia (NCAR WRF physics report 2000)
 
 ! Key:
 
 !-----------------------------------------------------------------------
@@ -6910,56 +5628,48 @@ END SUBROUTINE nonlocal_flux
     INTEGER, INTENT( IN )  &
     :: ids, ide, jds, jde, kds, kde,  &
        ims, ime, jms, jme, kms, kme,  &
        its, ite, jts, jte, kts, kte
 
     INTEGER, INTENT( IN )  &
     :: is_ext, ie_ext, js_ext, je_ext
 
     REAL, DIMENSION( kms:kme ), INTENT( IN )  &
     :: fnm, fnp
 
     REAL, DIMENSION( its-1:ite+1, kts:kte, jts-1:jte+1 ), INTENT( INOUT )  &
     :: titau
 
     REAL, DIMENSION( ims:ime, kms:kme, jms:jme), INTENT( IN )  &
     :: defor, xkx, rho
 
     REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT( INOUT )  &
     :: mtau
 
-    REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT( IN )  &
-    :: rtau   ! XS
-
-    REAL , DIMENSION(ims:ime, kms:kme, jms:jme), INTENT( IN ) &
-    :: ctau13
-
 ! Local variables.
 
     INTEGER  &
     :: i, j, k, ktf, i_start, i_end, j_start, j_end
 
     REAL, DIMENSION( its-1:ite+1, kts:kte, jts-1:jte+1 )  &
     :: xkxavg, rhoavg
 
-!	REAL :: rhoavg   ! XS 
-
 ! End declarations.
 !-----------------------------------------------------------------------
 
     ktf = MIN( kte, kde-1 )
 
 ! Find ide-1 and jde-1 for averaging to p point.
 
     i_start = its
     i_end   = ite
     j_start = jts
     j_end   = MIN( jte, jde-1 )
 
     IF ( config_flags%open_xs .OR. config_flags%specified .OR. &
          config_flags%nested) i_start = MAX( ids+1, its )
     IF ( config_flags%open_xe .OR. config_flags%specified .OR. &
          config_flags%nested) i_end   = MIN( ide-1, ite )
     IF ( config_flags%open_ys .OR. config_flags%specified .OR. &
          config_flags%nested) j_start = MAX( jds+1, jts )
     IF ( config_flags%open_ye .OR. config_flags%specified .OR. &
          config_flags%nested) j_end   = MIN( jde-2, jte )
@@ -7007,94 +5717,55 @@ END SUBROUTINE nonlocal_flux
         ENDDO
         ENDDO
         ENDDO
 
       ELSE ! NO STRESS OUTPUT
 
         DO j = j_start, j_end
         DO k = kts+1, ktf
         DO i = i_start, i_end
 
           titau(i,k,j) = - xkxavg(i,k,j) * defor(i,k,j)
 
         ENDDO
         ENDDO
         ENDDO
 
       ENDIF
 
     ENDIF
 
-
-    IF ( config_flags%rsfs_opt .lt. 6  .and. config_flags%rsfs_opt .ge. 0) THEN ! add RSFS
-                                                                                          
-      DO j = j_start, j_end                                                               
-      DO k = kts+1, ktf                                                                   
-      DO i = i_start, i_end                                                               
-!         rhoavg = 0.5 * ( fnm(k) * ( rho(i-1,k  ,j) + rho(i,k  ,j) ) + &                  
-!                          fnp(k) * ( rho(i-1,k-1,j) + rho(i,k-1,j) ) )      
-         if ((k-kts).lt.config_flags%drm_dwl_klevel .or. config_flags%drm_dwl_klevel.eq.0) then ! ASW don't add rsfs above some k level for numerical stability
-
-         !! ASW DWL the easy way
-		 !IF ( config_flags%rsfsU_opt .eq. 1) THEN
-		 titau(i,k,j) = titau(i,k,j) + rhoavg(i,k,j)*rtau(i,k,j) 
-		 !ELSE IF ( config_flags%rsfsU_opt .eq. 0) THEN
-		 !titau(i,k,j) = titau(i,k,j)
-		 !END IF
-
-         end if
-      END DO                                                                              
-      END DO                                                                              
-      END DO                                                                              
-                                                                                          
-   ENDIF ! RSFS                                                                           
-                                                                                          
-    IF ( config_flags%can_opt .EQ. 1 ) THEN ! ADD CANOPY STRESSES                         
-                                                                                          
-       DO j = j_start, j_end                                                              
-       DO k = kts+1, ktf                                                                  
-       DO i = i_start, i_end                                                              
-!          rhoavg = 0.5 * ( fnm(k) * ( rho(i-1,k  ,j) + rho(i,k  ,j) ) + &                 
-!                           fnp(k) * ( rho(i-1,k-1,j) + rho(i,k-1,j) ) )                   
-          titau(i,k,j) = titau(i,k,j) + rhoavg(i,k,j)*ctau13(i,k,j)                              
-       END DO                                                                             
-       END DO                                                                             
-       END DO                                                                             
-                                                                                          
-   ENDIF ! CAN                                                                            
-
     DO j = j_start, j_end
     DO i = i_start, i_end
       titau(i,kts  ,j) = 0.0
       titau(i,ktf+1,j) = 0.0
     ENDDO
     ENDDO
 
     END SUBROUTINE cal_titau_13_31
 
 !=======================================================================
 !=======================================================================
 
     SUBROUTINE cal_titau_23_32( config_flags, titau, defor,      &
-                                mtau, rtau,                      &   ! XS
-                                ctau23,                          & !JDM 
-								xkx, fnm, fnp, rho,              &
+                                mtau,                            &
+                                xkx, fnm, fnp, rho,              &
                                 is_ext, ie_ext, js_ext, je_ext,  &
                                 ids, ide, jds, jde, kds, kde,    &
                                 ims, ime, jms, jme, kms, kme,    &
                                 its, ite, jts, jte, kts, kte     )
 
 ! History:     Sep 2003  Changes by George Bryan and Jason Knievel, NCAR
 !              Oct 2001  Converted to mass core by Bill Skamarock, NCAR
 !              Aug 2000  Original code by Shu-Hua Chen, UC-Davis
 
 ! Purpose:     This routine calculates stress terms (taus) for use in
 !              the calculation of production of TKE by sheared wind
 
 ! References:  Klemp and Wilhelmson (JAS 1978)
 !              Deardorff (B-L Meteor 1980)
 !              Chen and Dudhia (NCAR WRF physics report 2000)
 
 ! Key:
 
 !-----------------------------------------------------------------------
 ! Begin declarations.
@@ -7107,49 +5778,40 @@ END SUBROUTINE nonlocal_flux
     INTEGER, INTENT( IN )  &
     :: ids, ide, jds, jde, kds, kde,  &
        ims, ime, jms, jme, kms, kme,  &
        its, ite, jts, jte, kts, kte
 
     INTEGER, INTENT( IN )  &
     :: is_ext,ie_ext,js_ext,je_ext
 
     REAL, DIMENSION( kms:kme ), INTENT( IN )  &
     :: fnm, fnp
 
     REAL, DIMENSION( its-1:ite+1, kts:kte, jts-1:jte+1 ), INTENT( INOUT )  &
     :: titau
 
     REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT( IN )  &
     :: defor, xkx, rho
 
     REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT( INOUT )  &
     :: mtau
 
-    REAL, DIMENSION( ims:ime, kms:kme, jms:jme ), INTENT( IN )  &
-    :: rtau   ! XS
-
-    REAL , DIMENSION(ims:ime, kms:kme, jms:jme), INTENT( IN ) &
-    :: ctau23
-
-!	REAL :: rhoavg   
-
-
 ! Local variables.
 
     INTEGER  &
     :: i, j, k, ktf, i_start, i_end, j_start, j_end
 
     REAL, DIMENSION( its-1:ite+1, kts:kte, jts-1:jte+1 )  &
     :: xkxavg, rhoavg
 
 ! End declarations.
 !-----------------------------------------------------------------------
 
      ktf = MIN( kte, kde-1 )
 
 ! Find ide-1 and jde-1 for averaging to p point.
 
     i_start = its
     i_end   = MIN( ite, ide-1 )
     j_start = jts
     j_end   = jte
 
@@ -7207,78 +5869,40 @@ END SUBROUTINE nonlocal_flux
         END DO
         END DO
         END DO
 
       ELSE ! NO STRESS OUTPUT
 
         DO j = j_start, j_end
         DO k = kts+1, ktf
         DO i = i_start, i_end
 
           titau(i,k,j) = -  xkxavg(i,k,j) * defor(i,k,j)
 
         END DO
         END DO
         END DO
 
       ENDIF
 
     ENDIF
 
-
-    IF ( config_flags%rsfs_opt .lt. 6 .and. config_flags%rsfs_opt .ge. 0) THEN ! add RSFS   
-                                                                                            
-      DO j = j_start, j_end                                                                 
-      DO k = kts+1, ktf                                                                     
-      DO i = i_start, i_end                                                                 
-!         rhoavg = 0.5 * ( fnm(k) * ( rho(i,k  ,j) + rho(i,k  ,j-1) ) +  &                   
-!                          fnp(k) * ( rho(i,k-1,j) + rho(i,k-1,j-1) ) )   
-        if ((k-kts).lt.config_flags%drm_dwl_klevel .or. config_flags%drm_dwl_klevel.eq.0) then ! ASW don't add rsfs above some k level for numerical stability
-
-		!! ASW DWL the easy way
-		!IF ( config_flags%rsfsU_opt .eq. 1) THEN
-		titau(i,k,j) = titau(i,k,j) + rhoavg(i,k,j)*rtau(i,k,j)                                   
-		!ELSE IF ( config_flags%rsfsU_opt .eq. 0) THEN
-		!titau(i,k,j) = titau(i,k,j)
-		!END IF
-
-        end if			
-	  END DO                                                                                
-      END DO                                                                                
-      END DO                                                                                
-                                                                                            
-    ENDIF ! RSFS                                                                            
-                                                                                            
-     IF ( config_flags%can_opt .EQ. 1 ) THEN ! ADD CANOPY STRESSES                          
-                                                                                            
-       DO j = j_start, j_end                                                                
-       DO k = kts+1, ktf                                                                    
-       DO i = i_start, i_end                                                                
-!          rhoavg = 0.5 * ( fnm(k) * ( rho(i,k  ,j) + rho(i,k  ,j-1) ) +  &                  
-!                           fnp(k) * ( rho(i,k-1,j) + rho(i,k-1,j-1) ) )                     
-          titau(i,k,j) = titau(i,k,j) + rhoavg(i,k,j)*ctau23(i,k,j)                                
-       END DO                                                                               
-       END DO                                                                               
-       END DO                                                                               
-                                                                                            
-    ENDIF ! CAN                                                                             
-
     DO j = j_start, j_end
     DO i = i_start, i_end
       titau(i,kts  ,j) = 0.0
       titau(i,ktf+1,j) = 0.0
     END DO
     END DO
 
     END SUBROUTINE cal_titau_23_32
 
 !=======================================================================
 !=======================================================================
 
 SUBROUTINE phy_bc ( config_flags,div,defor11,defor22,defor33,              &
                     defor12,defor13,defor23,xkmh,xkmv,xkhh,xkhv,tke,rho,   &
                     RUBLTEN, RVBLTEN,                                      &
                     RUCUTEN, RVCUTEN,                                      &
                     RUSHTEN, RVSHTEN,                                      &
                     gamu, gamv, xkmv_meso,                                 & ! XZ
                     ids, ide, jds, jde, kds, kde,                          &
                     ims, ime, jms, jme, kms, kme,                          &
@@ -7452,40 +6076,481 @@ SUBROUTINE phy_bc ( config_flags,div,defor11,defor22,defor33,              &
    IF(config_flags%diff_opt .eq. 2 .and. config_flags%km_opt .eq. 5) THEN
    CALL set_physical_bc2d( gamu, 't', config_flags,                    &
                                    ids, ide, jds, jde,                 &
                                    ims, ime, jms, jme,                 &
                                    ips, ipe, jps, jpe,                 &
                                    its, ite, jts, jte                  )
    CALL set_physical_bc2d( gamv, 't', config_flags,                    &
                                    ids, ide, jds, jde,                 &
                                    ims, ime, jms, jme,                 &
                                    ips, ipe, jps, jpe,                 &
                                    its, ite, jts, jte                  )
    CALL set_physical_bc3d( xkmv_meso,'t', config_flags,                &
                                 ids, ide, jds, jde, kds, kde,          &
                                 ims, ime, jms, jme, kms, kme,          &
                                 ips, ipe, jps, jpe, kps, kpe,          &
                                 its, ite, jts, jte, kts, kte           )
    ENDIF
 !
 END SUBROUTINE phy_bc
 
+!=======================================================================
+!=======================================================================
+
+SUBROUTINE phy_bc_state_pbl3d ( config_flags, u_2, v_2, w_2,     &
+                                t_2, thetav, moist, rho,         &
+                                q_sq_prog, q_sq, l_master,       &
+                                ids, ide, jds, jde, kds, kde,    &
+                                ims, ime, jms, jme, kms, kme,    &
+                                ips, ipe, jps, jpe, kps, kpe,    &
+                                its, ite, jts, jte, kts, kte     )
+
+!------------------------------------------------------------------------------
+! Begin declarations.
+
+   IMPLICIT NONE
+
+   TYPE(grid_config_rec_type), INTENT(IN   ) :: config_flags
+
+   INTEGER ,        INTENT(IN   ) ::        ids, ide, jds, jde, kds, kde, &
+                                            ims, ime, jms, jme, kms, kme, &
+                                            ips, ipe, jps, jpe, kps, kpe, &
+                                            its, ite, jts, jte, kts, kte
+
+   REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(INOUT) :: u_2,       &
+                                                                  v_2,       &
+                                                                  w_2,       &
+                                                                  t_2,       &
+                                                                  thetav,    &
+                                                                  moist,     &
+                                                                  rho,       &
+                                                                  q_sq_prog, &
+                                                                  q_sq,      &
+                                                                  l_master
+
+! End declarations.
+!-----------------------------------------------------------------------
+
+   CALL set_physical_bc3d( u_2, 'u', config_flags,                   &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( v_2, 'v', config_flags,                   &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( w_2, 'w', config_flags,                   &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( t_2, 't', config_flags,                   &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( thetav, 't', config_flags,                &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( moist, 't', config_flags,                 &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( rho, 't', config_flags,                   &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( q_sq_prog, 't', config_flags,             &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( q_sq, 'w', config_flags,                  &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( l_master, 'w', config_flags,              &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+
+END SUBROUTINE phy_bc_state_pbl3d
+
+!=======================================================================
+!=======================================================================
+
+SUBROUTINE phy_bc_pbl3d ( config_flags,                            &
+                          turb_flux_u2, turb_flux_v2,              &
+                          turb_flux_w2, turb_flux_uv,              &
+                          turb_flux_uw, turb_flux_vw,              &
+                          turb_flux_utheta, turb_flux_vtheta,      &
+                          turb_flux_wtheta, turb_flux_uqv,         &
+                          turb_flux_vqv, turb_flux_wqv,            &
+                          ids, ide, jds, jde, kds, kde,            &
+                          ims, ime, jms, jme, kms, kme,            &
+                          ips, ipe, jps, jpe, kps, kpe,            &
+                          its, ite, jts, jte, kts, kte             )
+
+!------------------------------------------------------------------------------
+! Begin declarations.
+
+   IMPLICIT NONE
+
+   TYPE(grid_config_rec_type), INTENT(IN   ) :: config_flags
+
+   INTEGER ,        INTENT(IN   ) ::        ids, ide, jds, jde, kds, kde, &
+                                            ims, ime, jms, jme, kms, kme, &
+                                            ips, ipe, jps, jpe, kps, kpe, &
+                                            its, ite, jts, jte, kts, kte
+
+   REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(INOUT) :: turb_flux_u2, &
+                                                                  turb_flux_v2, &
+                                                                  turb_flux_w2, &
+                                                                  turb_flux_uv, &
+                                                                  turb_flux_uw, &
+                                                                  turb_flux_vw, &
+                                                              turb_flux_utheta, &
+                                                              turb_flux_vtheta, &
+                                                              turb_flux_wtheta, &
+                                                                 turb_flux_uqv, &
+                                                                 turb_flux_vqv, &
+                                                                 turb_flux_wqv
+
+! End declarations.
+!-----------------------------------------------------------------------
+
+   CALL set_physical_bc3d( turb_flux_u2, 'w', config_flags,          &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_v2, 'w', config_flags,          &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_w2, 'w', config_flags,          &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_uv, 'w', config_flags,          &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_uw, 'w', config_flags,          &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_vw, 'w', config_flags,          &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_utheta, 'w', config_flags,      &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_vtheta, 'w', config_flags,      &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_wtheta, 'w', config_flags,      &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_uqv, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_vqv, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_wqv, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+END SUBROUTINE phy_bc_pbl3d
+
+!=======================================================================
+!=======================================================================
+
+SUBROUTINE scalar_bc_pbl3d ( config_flags,                                &
+                             turb_flux_uqc, turb_flux_vqc, turb_flux_wqc, &
+                             turb_flux_uqr, turb_flux_vqr, turb_flux_wqr, &
+                             turb_flux_uqi, turb_flux_vqi, turb_flux_wqi, &
+                             turb_flux_uqs, turb_flux_vqs, turb_flux_wqs, &
+                             turb_flux_uqg, turb_flux_vqg, turb_flux_wqg, &
+                             turb_flux_uqnc, turb_flux_vqnc, turb_flux_wqnc,  &
+                             turb_flux_uqnr, turb_flux_vqnr, turb_flux_wqnr,  &
+                             turb_flux_uqni, turb_flux_vqni, turb_flux_wqni,  &
+                             turb_flux_uqnwfa, turb_flux_vqnwfa, turb_flux_wqnwfa, &
+                             turb_flux_uqnifa, turb_flux_vqnifa, turb_flux_wqnifa, &
+                             ids, ide, jds, jde, kds, kde,                &
+                             ims, ime, jms, jme, kms, kme,                &
+                             ips, ipe, jps, jpe, kps, kpe,                &
+                             its, ite, jts, jte, kts, kte                 )
+
+!------------------------------------------------------------------------------
+! Begin declarations.
+
+   IMPLICIT NONE
+
+   TYPE(grid_config_rec_type), INTENT(IN   ) :: config_flags
+
+   INTEGER ,        INTENT(IN   ) ::        ids, ide, jds, jde, kds, kde, &
+                                            ims, ime, jms, jme, kms, kme, &
+                                            ips, ipe, jps, jpe, kps, kpe, &
+                                            its, ite, jts, jte, kts, kte
+
+   REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(INOUT) :: turb_flux_uqc, &
+      turb_flux_vqc, turb_flux_wqc, turb_flux_uqr, turb_flux_vqr, turb_flux_wqr, &
+      turb_flux_uqi, turb_flux_vqi, turb_flux_wqi, turb_flux_uqs, turb_flux_vqs, &
+      turb_flux_wqs, turb_flux_uqg, turb_flux_vqg, turb_flux_wqg, turb_flux_uqnc, &
+      turb_flux_vqnc, turb_flux_wqnc, turb_flux_uqnr, turb_flux_vqnr, turb_flux_wqnr, &
+      turb_flux_uqni, turb_flux_vqni, turb_flux_wqni, turb_flux_uqnwfa, turb_flux_vqnwfa, &
+      turb_flux_wqnwfa, turb_flux_uqnifa, turb_flux_vqnifa, turb_flux_wqnifa
+
+! End declarations.
+!-----------------------------------------------------------------------
+
+   CALL set_physical_bc3d( turb_flux_uqc, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_vqc, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_wqc, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_uqr, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_vqr, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_wqr, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_uqi, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_vqi, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_wqi, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_uqs, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_vqs, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_wqs, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_uqg, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_vqg, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_wqg, 'w', config_flags,         &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_uqnc, 'w', config_flags,        &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_vqnc, 'w', config_flags,        &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_wqnc, 'w', config_flags,        &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_uqnr, 'w', config_flags,        &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_vqnr, 'w', config_flags,        &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_wqnr, 'w', config_flags,        &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_uqni, 'w', config_flags,        &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_vqni, 'w', config_flags,        &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_wqni, 'w', config_flags,        &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_uqnwfa, 'w', config_flags,      &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_vqnwfa, 'w', config_flags,      &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_wqnwfa, 'w', config_flags,      &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_uqnifa, 'w', config_flags,      &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_vqnifa, 'w', config_flags,      &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+   CALL set_physical_bc3d( turb_flux_wqnifa, 'w', config_flags,      &
+                           ids, ide, jds, jde, kds, kde,             &
+                           ims, ime, jms, jme, kms, kme,             &
+                           ips, ipe, jps, jpe, kps, kpe,             &
+                           its, ite, jts, jte, kts, kte              )
+
+END SUBROUTINE scalar_bc_pbl3d
+
 !=======================================================================
 !=======================================================================
 
     SUBROUTINE tke_rhs( tendency, BN2, config_flags,            &
                         defor11, defor22, defor33,              &
                         defor12, defor13, defor23,              &
                         u, v, w, div, tke, mu, c1, c2,          &
                         theta, p, p8w, t8w, z, fnm, fnp,        &
                         cf1, cf2, cf3, msftx, msfty,            &
                         xkmh, xkmv, xkhv,                       & 
                         rdx, rdy, dx, dy, dt, zx, zy,           &
                         rdz, rdzw, dn, dnw, isotropic,          &
                         hfx, qfx, qv, ust, rho,                 &
                         l_diss, nlflux, hpbl, dlk,              & !XZ
                         ids, ide, jds, jde, kds, kde,           &
                         ims, ime, jms, jme, kms, kme,           &
                         its, ite, jts, jte, kts, kte            )
 
 !-----------------------------------------------------------------------
 ! Begin declarations.
@@ -7712,41 +6777,41 @@ END SUBROUTINE phy_bc
 !   flux at the first w level
 !
 ! WCS 040331
 
   hflux: SELECT CASE( config_flags%isfflx )
   CASE (0,2) ! with fixed surface heat flux given in the namelist
    heat_flux0 = config_flags%tke_heat_flux  ! constant heat flux value
                                             ! set in namelist.input
 ! LES mods
    K=KTS
 
    DO j = j_start, j_end
    DO i = i_start, i_end
       heat_flux = heat_flux0
       tendency(i,k,j)= tendency(i,k,j) - &
                     (c1(k)*mu(i,j)+c2(k))*((xkhv(i,k,j)*BN2(i,k,j))- (g/theta(i,k,j))*heat_flux)/2.
 
    ENDDO
    ENDDO
 
-  CASE (1,3) ! use surface heat flux computed from surface routine
+  CASE (1) ! use surface heat flux computed from surface routine
    K=KTS
 
    DO j = j_start, j_end
    DO i = i_start, i_end
       cpm = cp * (1. + 0.8*qv(i,k,j))
       heat_flux = (hfx(i,j)/cpm)/rho(i,k,j)
 
       tendency(i,k,j)= tendency(i,k,j) - &
                  (c1(k)*mu(i,j)+c2(k))*((xkhv(i,k,j)*BN2(i,k,j))- (g/theta(i,k,j))*heat_flux)/2.
    ENDDO
    ENDDO
 
   CASE DEFAULT
     CALL wrf_error_fatal( 'isfflx value invalid for diff_opt=2' )
   END SELECT hflux
 
 ! end of MARTA/WCS change
 
 ! The tendency array now includes production of TKE from buoyant
 ! motions.
@@ -8140,41 +7205,41 @@ END SUBROUTINE phy_bc
 !MARTA: add the drag at the surface; WCS 040331
     K=KTS
 
   uflux: SELECT CASE( config_flags%isfflx )
   CASE (0) ! Assume cd a constant, specified in namelist
 
     cd0 = config_flags%tke_drag_coefficient  ! drag coefficient set
                                              ! in namelist.input
     DO j = j_start, j_end
     DO i = i_start, i_end
 
      absU=0.5*sqrt((u(i,k,j)+u(i+1,k,j))**2+(v(i,k,j)+v(i,k,j+1))**2)
      Cd = cd0
      tendency(i,k,j) = tendency(i,k,j) +                     &
           (c1(k)*mu(i,j)+c2(k))*( (u(i,k,j)+u(i+1,k,j))*0.5* &
                    Cd*absU*(defor13(i,kts+1,j)+defor13(i+1,kts+1,j))*0.5 )
 
     END DO
     END DO
 
-  CASE (1,2,3) ! ustar computed from surface routine
+  CASE (1,2) ! ustar computed from surface routine
 
     DO j = j_start, j_end
     DO i = i_start, i_end
 
      absU=0.5*sqrt((u(i,k,j)+u(i+1,k,j))**2+(v(i,k,j)+v(i,k,j+1))**2)+epsilon
      Cd = (ust(i,j)**2)/(absU**2)
      tendency(i,k,j) = tendency(i,k,j) +                     &
           (c1(k)*mu(i,j)+c2(k))*( (u(i,k,j)+u(i+1,k,j))*0.5* &
                     Cd*absU*(defor13(i,kts+1,j)+defor13(i+1,kts+1,j))*0.5 )
 
     END DO
     END DO
 
   CASE DEFAULT
     CALL wrf_error_fatal( 'isfflx value invalid for diff_opt=2' )
   END SELECT uflux
 ! end of MARTA/WCS change
 
 ! For defor23.
 
@@ -8214,41 +7279,41 @@ END SUBROUTINE phy_bc
 !MARTA: add the drag at the surface; WCS 040331
     K=KTS
 
   vflux: SELECT CASE( config_flags%isfflx )
   CASE (0) ! Assume cd a constant, specified in namelist
 
     cd0 = config_flags%tke_drag_coefficient   ! constant drag coefficient
                                               ! set in namelist.input
     DO j = j_start, j_end
     DO i = i_start, i_end
 
       absU=0.5*sqrt((u(i,k,j)+u(i+1,k,j))**2+(v(i,k,j)+v(i,k,j+1))**2)
       Cd = cd0
       tendency(i,k,j) = tendency(i,k,j) +                     &
            (c1(k)*mu(i,j)+c2(k))*( (v(i,k,j)+v(i,k,j+1))*0.5* &
                      Cd*absU*(defor23(i,kts+1,j)+defor23(i,kts+1,j+1))*0.5 )
 
     END DO
     END DO
 
-  CASE (1,2,3) ! ustar computed from surface routine
+  CASE (1,2) ! ustar computed from surface routine
 
     DO j = j_start, j_end
     DO i = i_start, i_end
 
       absU=0.5*sqrt((u(i,k,j)+u(i+1,k,j))**2+(v(i,k,j)+v(i,k,j+1))**2)+epsilon
       Cd = (ust(i,j)**2)/(absU**2)
       tendency(i,k,j) = tendency(i,k,j) +       &
            (c1(k)*mu(i,j)+c2(k))*( (v(i,k,j)+v(i,k,j+1))*0.5* &
                      Cd*absU*(defor23(i,kts+1,j)+defor23(i,kts+1,j+1))*0.5 )
 
     END DO
     END DO
 
   CASE DEFAULT
     CALL wrf_error_fatal( 'isfflx value invalid for diff_opt=2' )
   END SELECT vflux
 ! end of MARTA/WCS change
 
     END SUBROUTINE tke_shear
 
@@ -8936,596 +8001,40 @@ END SUBROUTINE phy_bc
       DO j = jts, jte
       DO k = kts, kte
 !        rvort(ide,k,j) = rvort(ide-1,k,j)
         up_heli_max(ide,j) = up_heli_max(ide-1,j)
       END DO
       END DO
     END IF
 
     IF ( .NOT. config_flags%periodic_y .AND. j_end .EQ. jde-1) THEN
       DO k = kts, kte
       DO i = its, ite
 !        rvort(i,k,jde) = rvort(i,k,jde-1)
         up_heli_max(i,jde) = up_heli_max(i,jde-1)
       END DO
       END DO
     END IF
 
 ! End update of boundary for updraft helicity.
 
     END SUBROUTINE cal_helicity
-!=======================================================================
-!=======================================================================
-
-!JDM
-!=======================================================================
-
-!JDM-SWiFT Compute surface boundary conditions, START
-
-!=======================================================================
-
-
-SUBROUTINE calc_surf_ust_hs ( ust, hfx,                      &
-                              u_2, v_2, th, rdz,             &
-                              rho, tsk, cd, ch,              &
-                              moist, n_moist,                &
-                              dt, xtime,                     &
-                              heatflux,                      &
-                              zrough, z0,                    &
-                              !meso_force,                    & ! ASW remove meso scale options
-                              isfflx,                        &
-                              !meso_hfx, meso_tsk,            & ! ASW remove meso scale options
-                              ids, ide, jds, jde, kds, kde,  &
-                              ims, ime, jms, jme, kms, kme,  &
-                              its, ite, jts, jte, kts, kte   )
-
-!-----------------------------------------------------------------------
-!
-! Calculate surface forcing variables. Options include: 
-!
-!  1. Compute ustar and hfx using a skin temperature that is either 
-!    a) hardcoded into this subroutine
-!       --requires tke_heat_flux = -999.0 and meso_force = 0 in the namelist
-!    b) updated with mesoscale tendencies
-!       --requires meso_force = 2 in the namelist
-!
-!  2. Compute ustar using hfx that is either
-!    a) specified in the namelist 
-!       --requires tke_heat_flux = x, where x > -999.0, in the namelist 
-!    b) updated with mesoscale tendencies
-!       --requires meso_force = 1 in the namelist
-!
-! Begin declarations.
-
-   IMPLICIT NONE
-
-   INTEGER ,        INTENT(IN   ) ::        ids, ide, jds, jde, kds, kde, &
-                                            ims, ime, jms, jme, kms, kme, &
-                                            its, ite, jts, jte, kts, kte
-
-   REAL ,           INTENT(IN   ) ::        dt
-   REAL ,           INTENT(IN   ) ::        xtime
-   REAL ,           INTENT(IN   ) ::        heatflux
-   REAL ,           INTENT(IN   ) ::        zrough
-!   REAL ,           INTENT(IN   ) ::        meso_hfx
-!   REAL ,           INTENT(IN   ) ::        meso_tsk
-   INTEGER ,        INTENT(IN   ) ::        n_moist
-!   INTEGER ,        INTENT(IN   ) ::        meso_force
-   INTEGER ,        INTENT(IN   ) ::        isfflx
-
-   REAL , DIMENSION( ims:ime, kms:kme, jms:jme, n_moist),                 &
-          INTENT(IN) ::                                          moist           
-
-   REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(IN   ) :: rdz, &
-                                                                  u_2, &
-                                                                  v_2
-
-   REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(IN   ) :: th
-
-   REAL , DIMENSION( ims:ime, kms:kme, jms:jme), INTENT(IN   )   :: rho  
-   REAL , DIMENSION( ims:ime, jms:jme), INTENT(INOUT)            :: hfx
-   REAL , DIMENSION( ims:ime, jms:jme), INTENT(INOUT)            :: tsk
-   REAL , DIMENSION( ims:ime, jms:jme), INTENT(INOUT)            :: ust
-   REAL , DIMENSION( ims:ime, jms:jme), INTENT(INOUT)            :: z0
-   REAL , DIMENSION( ims:ime, jms:jme), INTENT(INOUT)            :: cd
-   REAL , DIMENSION( ims:ime, jms:jme), INTENT(INOUT)            :: ch
-
-! LOCAL VAR
-
-! les surface model constants (following Dyer and Hicks, 1974)
-!JDM   real, parameter :: epsi    = 1.e-6
-   real, parameter :: epsi    = 1.e-5
-   real, parameter :: beta    = 4.7 ! ASW Businger et al. 1971 to match Bowen ! JDM 5.
-   real, parameter :: gamma   = 15 ! ASW Businger et al. 1971 to match Bowen ! JDM 16.
-
-   INTEGER :: im, i, j, k, n
-   INTEGER :: i_start, i_end, j_start, j_end
-
-   INTEGER :: iter,max_iter
-
-!***************************************************************************
-!***************************************************************************
-!MODIFICA VARIABILI PER I FLUSSI
-!
-    REAL :: cpp
-    REAL :: ggg
-    REAL :: vk
-  
-    REAL :: u1
-    REAL :: v1
-    REAL :: z1
-    REAL :: th1
-    REAL :: rho1
-    REAL :: th0
-    REAL :: z0t
-    REAL :: ustar
-    REAL :: thstar
-    REAL :: wstar
-    REAL :: wth0
-    REAL :: t_0
-    REAL :: z1oz0
-    REAL :: z1oz0t
-    REAL :: molength
-    REAL :: wspd1
-    REAL :: tauxz
-    REAL :: tauyz
-    REAL :: zeta
-    REAL :: xi
-    REAL :: psim
-    REAL :: psih
-    REAL :: p_i
-    REAL :: cdn
-    REAL :: chn
-    REAL :: cd1
-    REAL :: ch1
-    REAL :: diff
-    REAL :: dtsdt     ! Specified surface temperature time rate of change [K s-1]
-    REAL :: dtsdmin   ! Specified surface temperature time rate of change [K min] ! ASW easier since xtime is in min
-    REAL :: t_start   ! Time in minutes from beginning of simulation to initialize tsk
-    REAL :: t_skin    ! Dummy variable for skin temperature [K]
-    REAL :: heat_flux ! Dummy variable for surface sensible heat flux [K m s-1]
-
-! End declarations.
-!-----------------------------------------------------------------------
-
-   i_start = its
-   i_end   = MIN(ite,ide-1)
-   j_start = jts
-   j_end   = MIN(jte,jde-1)
-
-   vk = 0.4
-   ggg = 9.81
-   cpp = 1004.6
-   t_0 = 300.0
-   p_i = 3.14159
-!   zrough = 0.05 !0.1 !0.05
-!   z0 = zrough
-!   z0t = zrough
-
-   
-
-
-! write(*,200),'TSK 1',tsk(its+2,jts+2),th(its+2,kts,jts+2)
-
-   heat_flux = heatflux
-
-   IF (heat_flux .LT. -0.001) THEN
-        dtsdmin = heat_flux/1000/60 ! ASW heat flux input divided by 1000 is K/hr cooling rate (divided by 60 for K/min)
-   ENDIF
-   t_start = 0.0
-   t_skin = 0.0
-
-!   IF (meso_force .EQ. 1 ) THEN
-
-!      heat_flux = meso_hfx
-
-!   ELSE IF (meso_force .EQ. 2 ) THEN  ! There is no handy way to compute surface pressure in ideal WRF
-                                      ! so instead meso_tsk is skin temperature from the meso run converted
-!      t_skin = meso_tsk               ! to potential temperature using the surface pressure in input_sounding
-
-!! Use this block of code to specify your own surface heating rate or to initialize tsk
-!!
-!!      IF ( ( xtime .GT. 119.999 ) .AND. ( xtime .LT. 120.001 ) )   THEN ! initialize ts to neutral at time t 
-!!
-!!         print*,'Initiaizing TSK to <TH1>_xy and ch and cd = 0.01'
-!!
-!!         DO j = j_start, j_end
-!!         DO i = i_start, i_end
-!!  
-!!            tsk(i,j) = 0.058 !0.9225 ! Compute from T in wrf history at time = t, enter here, and restart.
-!!
-!!         END DO
-!!         END DO
-!!
-!!      IF ( xtime .GT. 120.0 )  dtsdt = -0.00013888 !-0.00027777 !-0.000185185 ! -2/3 K/hr ! Assign cooling for greater than time t
-!!
-!   ELSE IF ( ( heat_flux .EQ. -999.0 ) .AND. (meso_force .NE. 2 ) ) THEN
-!
-!      IF ( xtime .GT. t_start ) THEN ! Begin cooling at specified time
-!
-!         dtsdt = -0.00013888 
-!
-!         IF ( ( xtime .GT. (t_start - 0.1*dt) ) .AND. ( xtime .LT. (t_start + 0.1*dt) ) ) THEN ! Optionally initialize tsk at time t 
-! 
-!             t_skin = 0.058 - dtsdt*dt 
-!
-!         ENDIF
-!  
-!         t_skin = t_skin + dtsdt*dt 
-!
-!      ENDIF
-
-!      ENDIF
-
-!print*,'heat_flux, t_skin ',heat_flux,t_skin
-
-! End use this block...
-
-
-! ASW attempt to cool surface
-
-
-
-
-   DO j = j_start, j_end
-   DO i = i_start, i_end
- 
-      z1 = 1.0/rdz(i,kts,j)
-
-      u1 = 0.5*( u_2(i,kts,j) + u_2(i+1,kts,j) ) 
-   
-      v1 = 0.5*( v_2(i,kts,j) + v_2(i,kts,j+1) ) 
-
-! ASW
-IF (heat_flux .LT. -0.001) THEN
-      !th0 = tsk(i,j) + dtsdmin*xtime ! Only for surface pressure = 1000 mb
-
-      th0 = dtsdmin*xtime + 300.0 ! ASW need to add initial temp here
-
-
-      !print*,'heat_flux, t_skin ',heat_flux,t_skin
-      th1 = th(i,kts,j)  + 300.0
-      !IF ( ( i .eq. its+5 ) .and. ( j .eq. jts+5 ) )  print*,'th0, th1, xtime ',th0,th1,xtime
-ENDIF
-! ASW
-
-      !th0 = t_skin ! Only for surface pressure = 1000 mb
-
-      !th1 = th(i,kts,j) + t0 ! Must add t0 to compare th1 with t_skin
-
-      rho1 = rho(i,kts,j)
-
-      ustar = ust(i,j)
-
-      z0(i,j) = zrough ! Seems wasteful to keep reassigning, but maintains flexibility 
-
-      z1oz0 = z1/z0(i,j)
-
-      z1oz0t = z1oz0 !z1/z0t
-
-      IF (cd(i,j) .EQ. 0.0) THEN ! First time called, so initialize coefficients
-
-          cd(i,j)=vk**2/((alog(z1oz0))**2)
-       
-          ch(i,j)=vk**2/((alog(z1oz0t))**2)
-
-      ENDIF
-
-! Wind speed at z1
-
-       wspd1 = SQRT(u1*u1+v1*v1 + 0.000001)
- 
-! Surface stress
-
-       cd1 = cd(i,j)
-
-       ch1 = ch(i,j)
-
-       tauxz = cd1*wspd1*u1
-
-       tauyz = cd1*wspd1*v1
-  
-       ustar = (tauxz**2+tauyz**2)**0.25
-
-       diff = 1.0
-
-! Surface sensible heat flux
-    
-       wth0 = ch1*wspd1*(th0-th1)
-   
-       !IF (heat_flux .NE. -999.0) wth0 = heat_flux/(cpp*rho1) ! Enforce namelist value, W/m^2 -> K m/s ! for constant value
-
-       ! ASW
-       IF (heat_flux .GT. -0.001) wth0 = heat_flux/(cpp*rho1) ! Enforce namelist value, W/m^2 -> K m/s ! for constant value
-
-!JDM Heat Flux Function Begin
-
-!          IF ( (xtime .GT. 899.999) .AND. (xtime .LT. 959.999) ) heat_flux = 100.0*((xtime-899.999)/60.0) ! Ramp up to 100 over 1 hour, hr 15-16
-
-!          IF ( (xtime .GE. 959.999) .AND. (xtime .LT. 1019.999) ) heat_flux = 100.000*((1019.999-xtime)/60.0) ! Ramp down to 0 over 1 hour, hr 16-17
-
-!          IF ( (xtime .GE. 1019.999) .AND. (xtime .LT. 1079.999 ) ) heat_flux = 10.000*((1019.999-xtime)/60.0) ! Ramp down to -10 over 1 hour, hr 17-18
-
-!          IF ( xtime .GE. 1079.999 ) heat_flux = -10.0 ! Constant value of -10 hereafter, hr > 18
-
-!          wth0 = heat_flux/(cpp*rho1) 
-
-!JDM Heat Flux Function Begin
-
-       iter = 0 
-
-!---- Start iterating ---------------------------------------------
-
-       do while ( diff .GT. epsi ) 
-
-! Surface parameters
-
-          if ( ustar .ne. 0.0 ) then 
-
-             thstar=wth0/ustar
-
-          else
-
-             thstar = 0.0
-
-          endif
-
-          if ( thstar .ne. 0.0 ) then 
-
-             wstar = wth0/thstar
-
-          else
-
-             wstar = 0.0
-
-          endif
-
-          if ( ( abs(wth0) .gt. 0.0 ) .and. ( ustar .gt. 0.0 ) ) then
-
-! Monin-Obukhov length scale 
-
-             molength = -((ustar**3)*t_0)/(vk*ggg*wth0)
-
-! M-O Stability paramter
-
-             zeta=z1/molength
-
-          endif
-
-! Surface heat flux = 0, friction velocity > 0
-
-          if ( ( abs(wth0) .eq. 0.0 ) .and. ( ustar .gt. 0.0 ) ) then
-
-             zeta = 0.0
-
-          endif
-
-! Free convection - this is temporary fix, needs to be modified
-
-          if ( ( abs(wth0) .ge. 0.0 ) .and. ( ustar .eq. 0.0 ) ) then
-
-             zeta = -3.0
-
-          endif
-
-! Surface heat flux = 0, friction velocity = 0
-
-          if ( ( abs(wth0) .eq. 0.0 ) .and. ( ustar .eq. 0.0 ) ) then
-
-             zeta = 0.0
-
-          endif
-
-!   WRITE(message,*)' z/L     = ',zeta
-!   CALL wrf_message(message)
-
-! Convective case
-
-          if ((zeta .lt. -epsi) .and. (zeta .ge. -2.0)) then
-
-!             print*,'convective,zeta,ch1,hfx(i,j)',zeta,ch1,wth0
-         
-            xi = 1.0/((1.0-gamma*zeta)**0.25)
-            psim = alog(0.5*(1.0+xi**2)*(0.5*(1.0+xi))**2) - 2.0*atan(xi)+0.5*p_i
-            psih = 2.0*alog(0.5*(1.0+xi**2))
-
-!   WRITE(message,*)' Convective case ' 
-!   CALL wrf_message(message)
-!   WRITE(message,*)' psim, psih, psiq ',psim,psih,psiq 
-!   CALL wrf_message(message)
-
-          endif
-
-! Stably statified case
-
-          if ((zeta .gt. +epsi) .and. (zeta .lt. 1.0)) then
-
-            !IF ( ( i .eq. its+5 ) .and. ( j .eq. jts+5 ) )  print*,'Stably stratified ',molength ! ASW
-            psim = -beta*zeta
-            psih = -beta*zeta
-
-!   WRITE(message,*)' Stable case ' 
-!   CALL wrf_message(message)
-!   WRITE(message,*)' psim, psih, psiq ',psim,psih,psiq 
-!   CALL wrf_message(message)
-
-          endif
-
-! Neutral case
-
-          if ((zeta .le. +epsi) .and. (zeta .ge. -epsi)) then
-
-            psim = 0.0
-            psih = 0.0
-
-!   WRITE(message,*)' Neutral case ' 
-!   CALL wrf_message(message)
-!   WRITE(message,*)' psim, psih, psiq ',psim,psih,psiq 
-!   CALL wrf_message(message)
-
-          endif
-
-! Drag coefficients
-
-          if ((zeta .gt. -2.0) .and. (zeta .lt. 1.0)) then
-
-            cdn = vk**2/((alog(z1oz0)-psim)**2)
-            chn = vk**2/((alog(z1oz0t)-psim)*(alog(z1oz0t)-psih))
-
-          else
-
-            cdn = 0.0
-            chn = 0.0
-
-          endif
-
-! Surface stress
-
-          tauxz = cdn*wspd1*u1
-          tauyz = cdn*wspd1*v1
-
-          ustar =(tauxz**2+tauyz**2)**0.25
-
-          !IF ( ( i .eq. its+5 ) .and. ( j .eq. jts+5 ) )  print*,'cdn,tauxz,tauyz,zeta,psim,psih ',cdn,tauxz,tauyz,zeta,psim,psih
-          !IF ( ( i .eq. its+5 ) .and. ( j .eq. jts+5 ) )  print*,'th0,ustar,wstar,thstar ',th0,ustar,wstar,thstar
-
-! Surface sensible heat flux
-
-          wth0 = chn*wspd1*(th0-th1)
-
-          !IF (heat_flux .NE. -999.0) wth0 = heat_flux/(cpp*rho1) ! Enforce namelist value, W/m^2 -> K m/s
-          ! ASW
-          IF (heat_flux .GT. -0.001) wth0 = heat_flux/(cpp*rho1) ! Enforce namelist value, W/m^2 -> K m/s ! for constant value
-
-!JDM Heat Flux Function Begin
-
-!          IF ( (xtime .GT. 899.999 ) .AND. (xtime .LT. 959.999 ) ) heat_flux = 100.0*((xtime-899.999)/60.0) ! Ramp up to 100 over 1 hour, hr 15-16
-
-!          IF ( (xtime .GE. 959.999) .AND. (xtime .LT. 1019.999 ) ) heat_flux = 100.000*((1019.999-xtime)/60.0) ! Ramp down to 0 over 1 hour, hr 16-17
-
-!          IF ( (xtime .GE. 1019.999) .AND. (xtime .LT. 1079.999 ) ) heat_flux = 10.000*((1019.999-xtime)/60.0) ! Ramp down to -10 over 1 hour, hr 17-18
-
-!          IF ( xtime .GE. 1079.999 ) heat_flux = -10.0 ! Constant value of -10 hereafter, hr > 18
-
-!          wth0 = heat_flux/(cpp*rho1) 
-
-!JDM Heat Flux Function End
-
-          diff = abs(cdn-cd1)+abs(chn-ch1)
-
-          cd1 = cdn
-          ch1 = chn
-
-          iter = iter + 1
-
-       IF (iter .EQ. 1000 ) THEN 
-
-          print*,'over 1000 iterations in calc_surf_ust_hs,i,j',i,j
-          print*,'cd1,cdn,ch1,chn',cd1,cdn,ch1,chn
-         
-          goto 100
-
-       ENDIF
-
-       enddo
-
-100 continue
-
-!---- End iterating (while loop) -------------------------------------
-
-      ust(i,j) = ustar
-      hfx(i,j) = wth0*cpp*rho1
-      tsk(i,j) = th0            !only for surface pressure = 1000 mb
-      cd(i,j) = cdn
-      ch(i,j) = chn
-
-!JDM     IF ( ( i .eq. its+5 ) .and. ( j .eq. jts+5 ) ) print*,ust(i,j),hfx(i,j),tsk(i,j)
-
-  ENDDO !i
-  ENDDO !j   
-
-!   write(*,200),'TSK 2',tsk(its+2,jts+2),cd(its+2,jts+2),ch(its+2,jts+2)
-
-!print*,'LEAVING SURFACE LAYER, ust, hfx,',ust(its+5,jts+5),hfx(its+5,jts+5)
-!print*,'LEAVING SURFACE LAYER, rho, z0',rho(its+5,kts,jts+5),z0(its+5,jts+5)
-
-200 format (a5,1x,3F20.16)
-
-END SUBROUTINE calc_surf_ust_hs
-
-!=======================================================================
-
-!JDM-SWiFT Compute surface boundary conditions, END
-
-!JDM-SWiFT Assign ustm to ust, BEGIN
-
-!=======================================================================
-
-    SUBROUTINE ustm_to_ust( ust, ustm,                            &
-                            ids, ide, jds, jde, kds, kde,         &
-                            ims, ime, jms, jme, kms, kme,         &
-                            its, ite, jts, jte, kts, kte          )
-
-!-----------------------------------------------------------------------
-! Begin declarations.
-
-    IMPLICIT NONE
-
-    INTEGER, INTENT( IN )  &
-    :: ids, ide, jds, jde, kds, kde,  &
-       ims, ime, jms, jme, kms, kme,  &
-       its, ite, jts, jte, kts, kte
-
-    REAL, DIMENSION( ims:ime, jms:jme ), INTENT( INOUT )  &
-    :: ustm
-
-    REAL, DIMENSION( ims:ime, jms:jme ), INTENT( OUT )  &
-    :: ust
-
-! Local variables.
-
-    INTEGER  &
-    :: i, j
-
-!    REAL  &
-!    :: ustmin
-
-
-! End declarations.
-!-----------------------------------------------------------------------
- 
-!    ustmin = 0.01
-
-    DO i = ims, ime
-    DO j = jms, jme
-
-!      ustm(i,j) =  MAX( ustm(i,j),ustmin )
-      ust(i,j)  =   ustm(i,j)
-
-    END DO
-    END DO
-
-    END SUBROUTINE ustm_to_ust
-
-!JDM
-
-
 !=======================================================================
 !=======================================================================
 
     SUBROUTINE tridiag(n,a,b,c,d)
 !! to solve system of linear eqs on tridiagonal matrix n times n
 !! after Peaceman and Rachford, 1955
 !! a,b,c,d - are vectors of order n 
 !! a,b,c - are coefficients on the LHS
 !! d - is initially RHS on the output becomes a solution vector
 !-------------------------------------------------------------------
  
     INTEGER, INTENT(IN)               :: n
     REAL, DIMENSION(n), INTENT(IN)    :: a,b
     REAL, DIMENSION(n), INTENT(INOUT) :: c,d
  
     INTEGER :: i
     REAL    :: p
     REAL, DIMENSION(n) :: q
  
     c(n) = 0.
@@ -10013,41 +8522,41 @@ END SUBROUTINE calc_surf_ust_hs
      DO j = j_start, j_end
      DO i = i_start, i_end
          xkxavg(i,kts  ,j) = 0.0
          xkxavg(i,ktf+1,j) = 0.0
      ENDDO
      ENDDO
  
     nz = ktf
  
   hflux: SELECT CASE( config_flags%isfflx )
   CASE (0,2) ! with fixed surface heat flux given in the namelist
     heat_flux = config_flags%tke_heat_flux  ! constant heat flux value
                                             ! set in namelist.input
     DO j = j_start, j_end
     DO i = i_start, i_end
        cpm = cp * (1. + 0.8 * moist(i,kts,j,P_QV))
        hfx(i,j)=heat_flux*cpm*rho(i,1,j)
     ENDDO
     ENDDO
  
-  CASE (1,3) ! use surface heat flux computed from surface routine
+  CASE (1) ! use surface heat flux computed from surface routine
     ! do nothing
   CASE DEFAULT
     CALL wrf_error_fatal( 'isfflx value invalid for diff_opt=2' )
   END SELECT hflux
 
     DO  j = j_start, j_end
     DO  i = i_start, i_end
             cpm = cp * (1. + 0.8 * moist(i,kts,j,P_QV))
             k = kts
             rdz_w = -g/dnw(k)/(c1h(k)*mu(i,j) + c2h(k))
             a(k) = 0.
             b(k) = 1. + rdz_w*(rdz(i,k+1,j)*xkxavg(i,k+1,j))*dt
             c(k) =    - rdz_w* rdz(i,k+1,j)*xkxavg(i,k+1,j) *dt
          
           IF(config_flags%use_theta_m == 0)THEN
              d(k) = var_mix(i,kts,j)                    &
                   - dt*rdz_w*nlflux_rho(i,k+1,j)        &
                   + dt*rdz_w*hfx(i,j)/cpm
           ELSE
              qfac = 1.+rvovrd*moist(i,kts,j,P_QV)
